// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               v7.35.1
// source: room.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "canvasphysics.v1";

export enum HostControlKind {
  HOST_CONTROL_UNSPECIFIED = 0,
  HOST_CONTROL_GRANTED = 1,
  HOST_CONTROL_REVOKED = 2,
  HOST_CONTROL_YIELD_REQUEST = 3,
  HOST_CONTROL_YIELD = 4,
  HOST_CONTROL_ELIGIBILITY = 5,
  UNRECOGNIZED = -1,
}

export function hostControlKindFromJSON(object: any): HostControlKind {
  switch (object) {
    case 0:
    case "HOST_CONTROL_UNSPECIFIED":
      return HostControlKind.HOST_CONTROL_UNSPECIFIED;
    case 1:
    case "HOST_CONTROL_GRANTED":
      return HostControlKind.HOST_CONTROL_GRANTED;
    case 2:
    case "HOST_CONTROL_REVOKED":
      return HostControlKind.HOST_CONTROL_REVOKED;
    case 3:
    case "HOST_CONTROL_YIELD_REQUEST":
      return HostControlKind.HOST_CONTROL_YIELD_REQUEST;
    case 4:
    case "HOST_CONTROL_YIELD":
      return HostControlKind.HOST_CONTROL_YIELD;
    case 5:
    case "HOST_CONTROL_ELIGIBILITY":
      return HostControlKind.HOST_CONTROL_ELIGIBILITY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return HostControlKind.UNRECOGNIZED;
  }
}

export function hostControlKindToJSON(object: HostControlKind): string {
  switch (object) {
    case HostControlKind.HOST_CONTROL_UNSPECIFIED:
      return "HOST_CONTROL_UNSPECIFIED";
    case HostControlKind.HOST_CONTROL_GRANTED:
      return "HOST_CONTROL_GRANTED";
    case HostControlKind.HOST_CONTROL_REVOKED:
      return "HOST_CONTROL_REVOKED";
    case HostControlKind.HOST_CONTROL_YIELD_REQUEST:
      return "HOST_CONTROL_YIELD_REQUEST";
    case HostControlKind.HOST_CONTROL_YIELD:
      return "HOST_CONTROL_YIELD";
    case HostControlKind.HOST_CONTROL_ELIGIBILITY:
      return "HOST_CONTROL_ELIGIBILITY";
    case HostControlKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Disposable presentation is deliberately absent from durable mutations;
 * ItemEditPreview has its own rate-limited realtime stream.
 */
export enum ItemMutationKind {
  ITEM_MUTATION_UNSPECIFIED = 0,
  ITEM_MUTATION_SPAWN = 1,
  ITEM_MUTATION_DELETE = 2,
  ITEM_MUTATION_TRANSFORM = 3,
  ITEM_MUTATION_CONFIG = 4,
  ITEM_MUTATION_ISOLATION = 5,
  ITEM_MUTATION_COLLISIONS = 6,
  ITEM_MUTATION_ROTATION = 7,
  ITEM_MUTATION_SCALE = 8,
  UNRECOGNIZED = -1,
}

export function itemMutationKindFromJSON(object: any): ItemMutationKind {
  switch (object) {
    case 0:
    case "ITEM_MUTATION_UNSPECIFIED":
      return ItemMutationKind.ITEM_MUTATION_UNSPECIFIED;
    case 1:
    case "ITEM_MUTATION_SPAWN":
      return ItemMutationKind.ITEM_MUTATION_SPAWN;
    case 2:
    case "ITEM_MUTATION_DELETE":
      return ItemMutationKind.ITEM_MUTATION_DELETE;
    case 3:
    case "ITEM_MUTATION_TRANSFORM":
      return ItemMutationKind.ITEM_MUTATION_TRANSFORM;
    case 4:
    case "ITEM_MUTATION_CONFIG":
      return ItemMutationKind.ITEM_MUTATION_CONFIG;
    case 5:
    case "ITEM_MUTATION_ISOLATION":
      return ItemMutationKind.ITEM_MUTATION_ISOLATION;
    case 6:
    case "ITEM_MUTATION_COLLISIONS":
      return ItemMutationKind.ITEM_MUTATION_COLLISIONS;
    case 7:
    case "ITEM_MUTATION_ROTATION":
      return ItemMutationKind.ITEM_MUTATION_ROTATION;
    case 8:
    case "ITEM_MUTATION_SCALE":
      return ItemMutationKind.ITEM_MUTATION_SCALE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ItemMutationKind.UNRECOGNIZED;
  }
}

export function itemMutationKindToJSON(object: ItemMutationKind): string {
  switch (object) {
    case ItemMutationKind.ITEM_MUTATION_UNSPECIFIED:
      return "ITEM_MUTATION_UNSPECIFIED";
    case ItemMutationKind.ITEM_MUTATION_SPAWN:
      return "ITEM_MUTATION_SPAWN";
    case ItemMutationKind.ITEM_MUTATION_DELETE:
      return "ITEM_MUTATION_DELETE";
    case ItemMutationKind.ITEM_MUTATION_TRANSFORM:
      return "ITEM_MUTATION_TRANSFORM";
    case ItemMutationKind.ITEM_MUTATION_CONFIG:
      return "ITEM_MUTATION_CONFIG";
    case ItemMutationKind.ITEM_MUTATION_ISOLATION:
      return "ITEM_MUTATION_ISOLATION";
    case ItemMutationKind.ITEM_MUTATION_COLLISIONS:
      return "ITEM_MUTATION_COLLISIONS";
    case ItemMutationKind.ITEM_MUTATION_ROTATION:
      return "ITEM_MUTATION_ROTATION";
    case ItemMutationKind.ITEM_MUTATION_SCALE:
      return "ITEM_MUTATION_SCALE";
    case ItemMutationKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ItemMutationRejectCode {
  ITEM_MUTATION_REJECT_UNSPECIFIED = 0,
  ITEM_MUTATION_REJECT_MALFORMED = 1,
  ITEM_MUTATION_REJECT_NOT_FOUND = 2,
  ITEM_MUTATION_REJECT_SYSTEM_OWNED = 3,
  ITEM_MUTATION_REJECT_NOT_OWNER = 4,
  ITEM_MUTATION_REJECT_EDIT_IN_USE = 5,
  ITEM_MUTATION_REJECT_EDIT_EXPIRED = 6,
  ITEM_MUTATION_REJECT_STALE_ITEM_REVISION = 7,
  ITEM_MUTATION_REJECT_OUTSIDE_CANVAS = 8,
  ITEM_MUTATION_REJECT_SCALE_OUT_OF_RANGE = 9,
  ITEM_MUTATION_REJECT_DEFINITION = 10,
  ITEM_MUTATION_REJECT_CONFIG = 11,
  ITEM_MUTATION_REJECT_CAPACITY = 12,
  ITEM_MUTATION_REJECT_RECEIPT_EXPIRED = 13,
  ITEM_MUTATION_REJECT_INTERNAL = 14,
  UNRECOGNIZED = -1,
}

export function itemMutationRejectCodeFromJSON(object: any): ItemMutationRejectCode {
  switch (object) {
    case 0:
    case "ITEM_MUTATION_REJECT_UNSPECIFIED":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_UNSPECIFIED;
    case 1:
    case "ITEM_MUTATION_REJECT_MALFORMED":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_MALFORMED;
    case 2:
    case "ITEM_MUTATION_REJECT_NOT_FOUND":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_NOT_FOUND;
    case 3:
    case "ITEM_MUTATION_REJECT_SYSTEM_OWNED":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_SYSTEM_OWNED;
    case 4:
    case "ITEM_MUTATION_REJECT_NOT_OWNER":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_NOT_OWNER;
    case 5:
    case "ITEM_MUTATION_REJECT_EDIT_IN_USE":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_EDIT_IN_USE;
    case 6:
    case "ITEM_MUTATION_REJECT_EDIT_EXPIRED":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_EDIT_EXPIRED;
    case 7:
    case "ITEM_MUTATION_REJECT_STALE_ITEM_REVISION":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_STALE_ITEM_REVISION;
    case 8:
    case "ITEM_MUTATION_REJECT_OUTSIDE_CANVAS":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_OUTSIDE_CANVAS;
    case 9:
    case "ITEM_MUTATION_REJECT_SCALE_OUT_OF_RANGE":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_SCALE_OUT_OF_RANGE;
    case 10:
    case "ITEM_MUTATION_REJECT_DEFINITION":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_DEFINITION;
    case 11:
    case "ITEM_MUTATION_REJECT_CONFIG":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_CONFIG;
    case 12:
    case "ITEM_MUTATION_REJECT_CAPACITY":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_CAPACITY;
    case 13:
    case "ITEM_MUTATION_REJECT_RECEIPT_EXPIRED":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_RECEIPT_EXPIRED;
    case 14:
    case "ITEM_MUTATION_REJECT_INTERNAL":
      return ItemMutationRejectCode.ITEM_MUTATION_REJECT_INTERNAL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ItemMutationRejectCode.UNRECOGNIZED;
  }
}

export function itemMutationRejectCodeToJSON(object: ItemMutationRejectCode): string {
  switch (object) {
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_UNSPECIFIED:
      return "ITEM_MUTATION_REJECT_UNSPECIFIED";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_MALFORMED:
      return "ITEM_MUTATION_REJECT_MALFORMED";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_NOT_FOUND:
      return "ITEM_MUTATION_REJECT_NOT_FOUND";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_SYSTEM_OWNED:
      return "ITEM_MUTATION_REJECT_SYSTEM_OWNED";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_NOT_OWNER:
      return "ITEM_MUTATION_REJECT_NOT_OWNER";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_EDIT_IN_USE:
      return "ITEM_MUTATION_REJECT_EDIT_IN_USE";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_EDIT_EXPIRED:
      return "ITEM_MUTATION_REJECT_EDIT_EXPIRED";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_STALE_ITEM_REVISION:
      return "ITEM_MUTATION_REJECT_STALE_ITEM_REVISION";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_OUTSIDE_CANVAS:
      return "ITEM_MUTATION_REJECT_OUTSIDE_CANVAS";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_SCALE_OUT_OF_RANGE:
      return "ITEM_MUTATION_REJECT_SCALE_OUT_OF_RANGE";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_DEFINITION:
      return "ITEM_MUTATION_REJECT_DEFINITION";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_CONFIG:
      return "ITEM_MUTATION_REJECT_CONFIG";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_CAPACITY:
      return "ITEM_MUTATION_REJECT_CAPACITY";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_RECEIPT_EXPIRED:
      return "ITEM_MUTATION_REJECT_RECEIPT_EXPIRED";
    case ItemMutationRejectCode.ITEM_MUTATION_REJECT_INTERNAL:
      return "ITEM_MUTATION_REJECT_INTERNAL";
    case ItemMutationRejectCode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ItemEditSessionStatus {
  ITEM_EDIT_SESSION_UNSPECIFIED = 0,
  ITEM_EDIT_SESSION_ACTIVE = 1,
  ITEM_EDIT_SESSION_RENEWED = 2,
  ITEM_EDIT_SESSION_ENDED = 3,
  ITEM_EDIT_SESSION_REJECTED = 4,
  ITEM_EDIT_SESSION_EXPIRED = 5,
  ITEM_EDIT_SESSION_SUPERSEDED = 6,
  UNRECOGNIZED = -1,
}

export function itemEditSessionStatusFromJSON(object: any): ItemEditSessionStatus {
  switch (object) {
    case 0:
    case "ITEM_EDIT_SESSION_UNSPECIFIED":
      return ItemEditSessionStatus.ITEM_EDIT_SESSION_UNSPECIFIED;
    case 1:
    case "ITEM_EDIT_SESSION_ACTIVE":
      return ItemEditSessionStatus.ITEM_EDIT_SESSION_ACTIVE;
    case 2:
    case "ITEM_EDIT_SESSION_RENEWED":
      return ItemEditSessionStatus.ITEM_EDIT_SESSION_RENEWED;
    case 3:
    case "ITEM_EDIT_SESSION_ENDED":
      return ItemEditSessionStatus.ITEM_EDIT_SESSION_ENDED;
    case 4:
    case "ITEM_EDIT_SESSION_REJECTED":
      return ItemEditSessionStatus.ITEM_EDIT_SESSION_REJECTED;
    case 5:
    case "ITEM_EDIT_SESSION_EXPIRED":
      return ItemEditSessionStatus.ITEM_EDIT_SESSION_EXPIRED;
    case 6:
    case "ITEM_EDIT_SESSION_SUPERSEDED":
      return ItemEditSessionStatus.ITEM_EDIT_SESSION_SUPERSEDED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ItemEditSessionStatus.UNRECOGNIZED;
  }
}

export function itemEditSessionStatusToJSON(object: ItemEditSessionStatus): string {
  switch (object) {
    case ItemEditSessionStatus.ITEM_EDIT_SESSION_UNSPECIFIED:
      return "ITEM_EDIT_SESSION_UNSPECIFIED";
    case ItemEditSessionStatus.ITEM_EDIT_SESSION_ACTIVE:
      return "ITEM_EDIT_SESSION_ACTIVE";
    case ItemEditSessionStatus.ITEM_EDIT_SESSION_RENEWED:
      return "ITEM_EDIT_SESSION_RENEWED";
    case ItemEditSessionStatus.ITEM_EDIT_SESSION_ENDED:
      return "ITEM_EDIT_SESSION_ENDED";
    case ItemEditSessionStatus.ITEM_EDIT_SESSION_REJECTED:
      return "ITEM_EDIT_SESSION_REJECTED";
    case ItemEditSessionStatus.ITEM_EDIT_SESSION_EXPIRED:
      return "ITEM_EDIT_SESSION_EXPIRED";
    case ItemEditSessionStatus.ITEM_EDIT_SESSION_SUPERSEDED:
      return "ITEM_EDIT_SESSION_SUPERSEDED";
    case ItemEditSessionStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Spec 12.3. One envelope carries every realtime message. Peers drop an
 * envelope whose host_epoch is older than the epoch they know.
 */
export interface RoomEnvelope {
  roomId: string;
  hostEpoch: number;
  sequence: number;
  tick: number;
  /** Client that sent the envelope. The server fills it on relay. */
  senderClientId: string;
  join?: Join | undefined;
  joinAccepted?: JoinAccepted | undefined;
  presence?: Presence | undefined;
  playerInput?: PlayerInput | undefined;
  stateDelta?: StateDelta | undefined;
  fullState?: FullState | undefined;
  effectEvent?: EffectEvent | undefined;
  hostControl?: HostControl | undefined;
  checkpoint?: Checkpoint | undefined;
  heartbeat?: Heartbeat | undefined;
  error?: ProtocolError | undefined;
  itemMutation?: ItemMutation | undefined;
  itemMutationResult?: ItemMutationResult | undefined;
  beginItemEdit?: BeginItemEdit | undefined;
  itemEditSessionResult?: ItemEditSessionResult | undefined;
  renewItemEdit?: RenewItemEdit | undefined;
  endItemEdit?: EndItemEdit | undefined;
  itemEditPreview?: ItemEditPreview | undefined;
}

export interface Vec2 {
  x: number;
  y: number;
}

export interface Join {
  roomId: string;
  protocolVersion: number;
  /** Item definition ids and versions the client already has. */
  definitions: DefinitionVersion[];
  /** A hidden client rejoins as ineligible instead of briefly winning a lease. */
  pageHidden: boolean;
}

export interface DefinitionVersion {
  definitionId: string;
  version: number;
}

export interface JoinAccepted {
  clientId: string;
  sceneRevision: number;
  hostEpoch: number;
  hostClientId: string;
  /** JSON canvas definition. Editable data stays JSON (spec 12.3). */
  canvasDefinitionJson: Uint8Array;
  /** Newest durable snapshot, as JSON. */
  snapshotJson: Uint8Array;
  roomWasSleeping: boolean;
  tickRate: number;
  /** Authenticated identity. Client-supplied JOIN identity is not trusted. */
  userId: string;
  displayName: string;
  /** Reusable template selected by the product for this room instance. */
  canvasId: string;
}

export interface Presence {
  peers: Peer[];
}

export interface Peer {
  clientId: string;
  userId: string;
  displayName: string;
  isHost: boolean;
  hostEligible: boolean;
}

export interface HostControl {
  kind: HostControlKind;
  hostClientId: string;
  hostEpoch: number;
  /** Set on HOST_CONTROL_GRANTED so the new host can rebuild the world. */
  snapshotJson: Uint8Array;
  reason: string;
  eligible: boolean;
  leaseExpiresAtUnixMs: number;
}

export interface Heartbeat {
  sentAtUnixMs: number;
  /** Host health signals used for election (spec 11.2). */
  simulationHz: number;
  workerDriftMs: number;
  pageVisible: boolean;
}

export interface PlayerInput {
  inputSequence: number;
  direction?: Vec2 | undefined;
  intensity: number;
  clientTimeUnixMs: number;
  /** True while the pointer is held, so a lost packet leaves no stale input. */
  held: boolean;
  /**
   * Addendum A1. True while the sender asks the host to disable its avatar.
   * The flag rides on every input, so a lost packet cannot leave a stale value.
   */
  avatarDisabled: boolean;
  /** Absolute world target for collision-safe direct avatar dragging. */
  targetPosition?: Vec2 | undefined;
}

export interface EntityState {
  entityId: string;
  quantizedTransform?:
    | QuantizedTransform
    | undefined;
  /** Set for avatars so the owner can reconcile its prediction. */
  lastProcessedInputSequence: number;
  spriteVariant: string;
  /** JSON behavior state, sent only when it changed. */
  behaviorStateJson: Uint8Array;
  quarantined: boolean;
  /** Lets a client that never held the item pick the right visual. */
  definitionId: string;
  /** Addendum A1. True when no physics act on this avatar. */
  disabled: boolean;
  /**
   * Addendum A2. Increases on every discontinuous move, such as an edge wrap or
   * a respawn. A client that sees a new value snaps instead of interpolating.
   */
  teleportEpoch: number;
  /** Addendum A3. True while the body waits out its respawn delay. */
  respawning: boolean;
  /** Named definition animation and a replay counter for repeated starts. */
  spriteAnimation: string;
  animationEpoch: number;
  /** True when the owner has removed this item from physics and behavior. */
  itemIsolated: boolean;
  /** Optional consumer-authored multiplicative sprite tint. */
  spriteTint: number;
  hasSpriteTint: boolean;
  /** True when this live item does not participate in contacts. */
  itemCollisionsDisabled: boolean;
}

export interface QuantizedTransform {
  /** Position and elevation use 1/100 canvas unit precision. */
  x: number;
  y: number;
  /** Rotation uses 1/1000 radian precision. */
  rotation: number;
  /** Linear velocity uses 1/100 canvas unit per second precision. */
  vx: number;
  vy: number;
  /** Angular velocity uses 1/1000 radian per second precision. */
  angularVelocity: number;
  z: number;
  vz: number;
  /** Uniform item scale uses 1/1000 precision. Avatars send 1.0. */
  scale: number;
}

export interface StateDelta {
  entities: EntityState[];
  removedEntityIds: string[];
  sceneRevision: number;
}

export interface FullState {
  entities: EntityState[];
  avatars: AvatarPresence[];
  sceneRevision: number;
  tickRate: number;
}

export interface AvatarPresence {
  entityId: string;
  clientId: string;
  userId: string;
  displayName: string;
}

export interface EffectEvent {
  entityId: string;
  effect: string;
  /** "oneShot", "start", or "stop". */
  mode: string;
  paramsJson: Uint8Array;
}

export interface ItemMutation {
  clientSessionId: string;
  mutationId: number;
  editSessionId: string;
  expectedItemRevision: number;
  kind: ItemMutationKind;
  entityId: string;
  definitionId: string;
  definitionVersion: number;
  position?: Vec2 | undefined;
  rotation: number;
  z: number;
  scale: number;
  configJson: Uint8Array;
  isolated: boolean;
  collisionsEnabled: boolean;
}

export interface ItemMutationResult {
  clientSessionId: string;
  mutationId: number;
  editSessionId: string;
  accepted: boolean;
  rejectCode: ItemMutationRejectCode;
  message: string;
  sceneRevision: number;
  itemRevision: number;
  itemInstanceJson: Uint8Array;
  deletedEntityId: string;
  kind: ItemMutationKind;
  entityId: string;
}

export interface BeginItemEdit {
  clientSessionId: string;
  editSessionId: string;
  entityId: string;
  observedItemRevision: number;
}

export interface RenewItemEdit {
  clientSessionId: string;
  editSessionId: string;
  entityId: string;
}

export interface EndItemEdit {
  clientSessionId: string;
  editSessionId: string;
  entityId: string;
  cancel: boolean;
}

export interface ItemEditSessionResult {
  clientSessionId: string;
  editSessionId: string;
  entityId: string;
  status: ItemEditSessionStatus;
  rejectCode: ItemMutationRejectCode;
  message: string;
  itemRevision: number;
  leaseExpiresAtUnixMs: number;
  itemInstanceJson: Uint8Array;
}

export interface ItemEditPreview {
  clientSessionId: string;
  editSessionId: string;
  entityId: string;
  previewSequence: number;
  position?: Vec2 | undefined;
  rotation: number;
  z: number;
  scale: number;
  /** Server-authored final sample that restores the committed transform. */
  revert: boolean;
}

export interface Checkpoint {
  checkpointRevision: number;
  tick: number;
  snapshotJson: Uint8Array;
  final: boolean;
}

export interface ProtocolError {
  code: string;
  message: string;
  serverProtocolVersion: number;
}

function createBaseRoomEnvelope(): RoomEnvelope {
  return {
    roomId: "",
    hostEpoch: 0,
    sequence: 0,
    tick: 0,
    senderClientId: "",
    join: undefined,
    joinAccepted: undefined,
    presence: undefined,
    playerInput: undefined,
    stateDelta: undefined,
    fullState: undefined,
    effectEvent: undefined,
    hostControl: undefined,
    checkpoint: undefined,
    heartbeat: undefined,
    error: undefined,
    itemMutation: undefined,
    itemMutationResult: undefined,
    beginItemEdit: undefined,
    itemEditSessionResult: undefined,
    renewItemEdit: undefined,
    endItemEdit: undefined,
    itemEditPreview: undefined,
  };
}

export const RoomEnvelope: MessageFns<RoomEnvelope> = {
  encode(message: RoomEnvelope, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.hostEpoch !== 0) {
      writer.uint32(16).uint64(message.hostEpoch);
    }
    if (message.sequence !== 0) {
      writer.uint32(24).uint32(message.sequence);
    }
    if (message.tick !== 0) {
      writer.uint32(32).uint64(message.tick);
    }
    if (message.senderClientId !== "") {
      writer.uint32(42).string(message.senderClientId);
    }
    if (message.join !== undefined) {
      Join.encode(message.join, writer.uint32(82).fork()).join();
    }
    if (message.joinAccepted !== undefined) {
      JoinAccepted.encode(message.joinAccepted, writer.uint32(90).fork()).join();
    }
    if (message.presence !== undefined) {
      Presence.encode(message.presence, writer.uint32(98).fork()).join();
    }
    if (message.playerInput !== undefined) {
      PlayerInput.encode(message.playerInput, writer.uint32(106).fork()).join();
    }
    if (message.stateDelta !== undefined) {
      StateDelta.encode(message.stateDelta, writer.uint32(114).fork()).join();
    }
    if (message.fullState !== undefined) {
      FullState.encode(message.fullState, writer.uint32(122).fork()).join();
    }
    if (message.effectEvent !== undefined) {
      EffectEvent.encode(message.effectEvent, writer.uint32(130).fork()).join();
    }
    if (message.hostControl !== undefined) {
      HostControl.encode(message.hostControl, writer.uint32(138).fork()).join();
    }
    if (message.checkpoint !== undefined) {
      Checkpoint.encode(message.checkpoint, writer.uint32(162).fork()).join();
    }
    if (message.heartbeat !== undefined) {
      Heartbeat.encode(message.heartbeat, writer.uint32(170).fork()).join();
    }
    if (message.error !== undefined) {
      ProtocolError.encode(message.error, writer.uint32(178).fork()).join();
    }
    if (message.itemMutation !== undefined) {
      ItemMutation.encode(message.itemMutation, writer.uint32(186).fork()).join();
    }
    if (message.itemMutationResult !== undefined) {
      ItemMutationResult.encode(message.itemMutationResult, writer.uint32(194).fork()).join();
    }
    if (message.beginItemEdit !== undefined) {
      BeginItemEdit.encode(message.beginItemEdit, writer.uint32(202).fork()).join();
    }
    if (message.itemEditSessionResult !== undefined) {
      ItemEditSessionResult.encode(message.itemEditSessionResult, writer.uint32(210).fork()).join();
    }
    if (message.renewItemEdit !== undefined) {
      RenewItemEdit.encode(message.renewItemEdit, writer.uint32(218).fork()).join();
    }
    if (message.endItemEdit !== undefined) {
      EndItemEdit.encode(message.endItemEdit, writer.uint32(226).fork()).join();
    }
    if (message.itemEditPreview !== undefined) {
      ItemEditPreview.encode(message.itemEditPreview, writer.uint32(234).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomEnvelope {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRoomEnvelope();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.roomId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.hostEpoch = longToNumber(reader.uint64());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.sequence = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.tick = longToNumber(reader.uint64());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.senderClientId = reader.string();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.join = Join.decode(reader, reader.uint32());
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.joinAccepted = JoinAccepted.decode(reader, reader.uint32());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.presence = Presence.decode(reader, reader.uint32());
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.playerInput = PlayerInput.decode(reader, reader.uint32());
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.stateDelta = StateDelta.decode(reader, reader.uint32());
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.fullState = FullState.decode(reader, reader.uint32());
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.effectEvent = EffectEvent.decode(reader, reader.uint32());
            continue;
          }
          case 17: {
            if (tag !== 138) {
              break;
            }

            message.hostControl = HostControl.decode(reader, reader.uint32());
            continue;
          }
          case 20: {
            if (tag !== 162) {
              break;
            }

            message.checkpoint = Checkpoint.decode(reader, reader.uint32());
            continue;
          }
          case 21: {
            if (tag !== 170) {
              break;
            }

            message.heartbeat = Heartbeat.decode(reader, reader.uint32());
            continue;
          }
          case 22: {
            if (tag !== 178) {
              break;
            }

            message.error = ProtocolError.decode(reader, reader.uint32());
            continue;
          }
          case 23: {
            if (tag !== 186) {
              break;
            }

            message.itemMutation = ItemMutation.decode(reader, reader.uint32());
            continue;
          }
          case 24: {
            if (tag !== 194) {
              break;
            }

            message.itemMutationResult = ItemMutationResult.decode(reader, reader.uint32());
            continue;
          }
          case 25: {
            if (tag !== 202) {
              break;
            }

            message.beginItemEdit = BeginItemEdit.decode(reader, reader.uint32());
            continue;
          }
          case 26: {
            if (tag !== 210) {
              break;
            }

            message.itemEditSessionResult = ItemEditSessionResult.decode(reader, reader.uint32());
            continue;
          }
          case 27: {
            if (tag !== 218) {
              break;
            }

            message.renewItemEdit = RenewItemEdit.decode(reader, reader.uint32());
            continue;
          }
          case 28: {
            if (tag !== 226) {
              break;
            }

            message.endItemEdit = EndItemEdit.decode(reader, reader.uint32());
            continue;
          }
          case 29: {
            if (tag !== 234) {
              break;
            }

            message.itemEditPreview = ItemEditPreview.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RoomEnvelope {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      hostEpoch: isSet(object.hostEpoch)
        ? globalThis.Number(object.hostEpoch)
        : isSet(object.host_epoch)
        ? globalThis.Number(object.host_epoch)
        : 0,
      sequence: isSet(object.sequence) ? globalThis.Number(object.sequence) : 0,
      tick: isSet(object.tick) ? globalThis.Number(object.tick) : 0,
      senderClientId: isSet(object.senderClientId)
        ? globalThis.String(object.senderClientId)
        : isSet(object.sender_client_id)
        ? globalThis.String(object.sender_client_id)
        : "",
      join: isSet(object.join) ? Join.fromJSON(object.join) : undefined,
      joinAccepted: isSet(object.joinAccepted)
        ? JoinAccepted.fromJSON(object.joinAccepted)
        : isSet(object.join_accepted)
        ? JoinAccepted.fromJSON(object.join_accepted)
        : undefined,
      presence: isSet(object.presence) ? Presence.fromJSON(object.presence) : undefined,
      playerInput: isSet(object.playerInput)
        ? PlayerInput.fromJSON(object.playerInput)
        : isSet(object.player_input)
        ? PlayerInput.fromJSON(object.player_input)
        : undefined,
      stateDelta: isSet(object.stateDelta)
        ? StateDelta.fromJSON(object.stateDelta)
        : isSet(object.state_delta)
        ? StateDelta.fromJSON(object.state_delta)
        : undefined,
      fullState: isSet(object.fullState)
        ? FullState.fromJSON(object.fullState)
        : isSet(object.full_state)
        ? FullState.fromJSON(object.full_state)
        : undefined,
      effectEvent: isSet(object.effectEvent)
        ? EffectEvent.fromJSON(object.effectEvent)
        : isSet(object.effect_event)
        ? EffectEvent.fromJSON(object.effect_event)
        : undefined,
      hostControl: isSet(object.hostControl)
        ? HostControl.fromJSON(object.hostControl)
        : isSet(object.host_control)
        ? HostControl.fromJSON(object.host_control)
        : undefined,
      checkpoint: isSet(object.checkpoint) ? Checkpoint.fromJSON(object.checkpoint) : undefined,
      heartbeat: isSet(object.heartbeat) ? Heartbeat.fromJSON(object.heartbeat) : undefined,
      error: isSet(object.error) ? ProtocolError.fromJSON(object.error) : undefined,
      itemMutation: isSet(object.itemMutation)
        ? ItemMutation.fromJSON(object.itemMutation)
        : isSet(object.item_mutation)
        ? ItemMutation.fromJSON(object.item_mutation)
        : undefined,
      itemMutationResult: isSet(object.itemMutationResult)
        ? ItemMutationResult.fromJSON(object.itemMutationResult)
        : isSet(object.item_mutation_result)
        ? ItemMutationResult.fromJSON(object.item_mutation_result)
        : undefined,
      beginItemEdit: isSet(object.beginItemEdit)
        ? BeginItemEdit.fromJSON(object.beginItemEdit)
        : isSet(object.begin_item_edit)
        ? BeginItemEdit.fromJSON(object.begin_item_edit)
        : undefined,
      itemEditSessionResult: isSet(object.itemEditSessionResult)
        ? ItemEditSessionResult.fromJSON(object.itemEditSessionResult)
        : isSet(object.item_edit_session_result)
        ? ItemEditSessionResult.fromJSON(object.item_edit_session_result)
        : undefined,
      renewItemEdit: isSet(object.renewItemEdit)
        ? RenewItemEdit.fromJSON(object.renewItemEdit)
        : isSet(object.renew_item_edit)
        ? RenewItemEdit.fromJSON(object.renew_item_edit)
        : undefined,
      endItemEdit: isSet(object.endItemEdit)
        ? EndItemEdit.fromJSON(object.endItemEdit)
        : isSet(object.end_item_edit)
        ? EndItemEdit.fromJSON(object.end_item_edit)
        : undefined,
      itemEditPreview: isSet(object.itemEditPreview)
        ? ItemEditPreview.fromJSON(object.itemEditPreview)
        : isSet(object.item_edit_preview)
        ? ItemEditPreview.fromJSON(object.item_edit_preview)
        : undefined,
    };
  },

  toJSON(message: RoomEnvelope): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.hostEpoch !== 0) {
      obj.hostEpoch = Math.round(message.hostEpoch);
    }
    if (message.sequence !== 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.tick !== 0) {
      obj.tick = Math.round(message.tick);
    }
    if (message.senderClientId !== "") {
      obj.senderClientId = message.senderClientId;
    }
    if (message.join !== undefined) {
      obj.join = Join.toJSON(message.join);
    }
    if (message.joinAccepted !== undefined) {
      obj.joinAccepted = JoinAccepted.toJSON(message.joinAccepted);
    }
    if (message.presence !== undefined) {
      obj.presence = Presence.toJSON(message.presence);
    }
    if (message.playerInput !== undefined) {
      obj.playerInput = PlayerInput.toJSON(message.playerInput);
    }
    if (message.stateDelta !== undefined) {
      obj.stateDelta = StateDelta.toJSON(message.stateDelta);
    }
    if (message.fullState !== undefined) {
      obj.fullState = FullState.toJSON(message.fullState);
    }
    if (message.effectEvent !== undefined) {
      obj.effectEvent = EffectEvent.toJSON(message.effectEvent);
    }
    if (message.hostControl !== undefined) {
      obj.hostControl = HostControl.toJSON(message.hostControl);
    }
    if (message.checkpoint !== undefined) {
      obj.checkpoint = Checkpoint.toJSON(message.checkpoint);
    }
    if (message.heartbeat !== undefined) {
      obj.heartbeat = Heartbeat.toJSON(message.heartbeat);
    }
    if (message.error !== undefined) {
      obj.error = ProtocolError.toJSON(message.error);
    }
    if (message.itemMutation !== undefined) {
      obj.itemMutation = ItemMutation.toJSON(message.itemMutation);
    }
    if (message.itemMutationResult !== undefined) {
      obj.itemMutationResult = ItemMutationResult.toJSON(message.itemMutationResult);
    }
    if (message.beginItemEdit !== undefined) {
      obj.beginItemEdit = BeginItemEdit.toJSON(message.beginItemEdit);
    }
    if (message.itemEditSessionResult !== undefined) {
      obj.itemEditSessionResult = ItemEditSessionResult.toJSON(message.itemEditSessionResult);
    }
    if (message.renewItemEdit !== undefined) {
      obj.renewItemEdit = RenewItemEdit.toJSON(message.renewItemEdit);
    }
    if (message.endItemEdit !== undefined) {
      obj.endItemEdit = EndItemEdit.toJSON(message.endItemEdit);
    }
    if (message.itemEditPreview !== undefined) {
      obj.itemEditPreview = ItemEditPreview.toJSON(message.itemEditPreview);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomEnvelope>, I>>(base?: I): RoomEnvelope {
    return RoomEnvelope.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomEnvelope>, I>>(object: I): RoomEnvelope {
    const message = createBaseRoomEnvelope();
    message.roomId = object.roomId ?? "";
    message.hostEpoch = object.hostEpoch ?? 0;
    message.sequence = object.sequence ?? 0;
    message.tick = object.tick ?? 0;
    message.senderClientId = object.senderClientId ?? "";
    message.join = (object.join !== undefined && object.join !== null) ? Join.fromPartial(object.join) : undefined;
    message.joinAccepted = (object.joinAccepted !== undefined && object.joinAccepted !== null)
      ? JoinAccepted.fromPartial(object.joinAccepted)
      : undefined;
    message.presence = (object.presence !== undefined && object.presence !== null)
      ? Presence.fromPartial(object.presence)
      : undefined;
    message.playerInput = (object.playerInput !== undefined && object.playerInput !== null)
      ? PlayerInput.fromPartial(object.playerInput)
      : undefined;
    message.stateDelta = (object.stateDelta !== undefined && object.stateDelta !== null)
      ? StateDelta.fromPartial(object.stateDelta)
      : undefined;
    message.fullState = (object.fullState !== undefined && object.fullState !== null)
      ? FullState.fromPartial(object.fullState)
      : undefined;
    message.effectEvent = (object.effectEvent !== undefined && object.effectEvent !== null)
      ? EffectEvent.fromPartial(object.effectEvent)
      : undefined;
    message.hostControl = (object.hostControl !== undefined && object.hostControl !== null)
      ? HostControl.fromPartial(object.hostControl)
      : undefined;
    message.checkpoint = (object.checkpoint !== undefined && object.checkpoint !== null)
      ? Checkpoint.fromPartial(object.checkpoint)
      : undefined;
    message.heartbeat = (object.heartbeat !== undefined && object.heartbeat !== null)
      ? Heartbeat.fromPartial(object.heartbeat)
      : undefined;
    message.error = (object.error !== undefined && object.error !== null)
      ? ProtocolError.fromPartial(object.error)
      : undefined;
    message.itemMutation = (object.itemMutation !== undefined && object.itemMutation !== null)
      ? ItemMutation.fromPartial(object.itemMutation)
      : undefined;
    message.itemMutationResult = (object.itemMutationResult !== undefined && object.itemMutationResult !== null)
      ? ItemMutationResult.fromPartial(object.itemMutationResult)
      : undefined;
    message.beginItemEdit = (object.beginItemEdit !== undefined && object.beginItemEdit !== null)
      ? BeginItemEdit.fromPartial(object.beginItemEdit)
      : undefined;
    message.itemEditSessionResult =
      (object.itemEditSessionResult !== undefined && object.itemEditSessionResult !== null)
        ? ItemEditSessionResult.fromPartial(object.itemEditSessionResult)
        : undefined;
    message.renewItemEdit = (object.renewItemEdit !== undefined && object.renewItemEdit !== null)
      ? RenewItemEdit.fromPartial(object.renewItemEdit)
      : undefined;
    message.endItemEdit = (object.endItemEdit !== undefined && object.endItemEdit !== null)
      ? EndItemEdit.fromPartial(object.endItemEdit)
      : undefined;
    message.itemEditPreview = (object.itemEditPreview !== undefined && object.itemEditPreview !== null)
      ? ItemEditPreview.fromPartial(object.itemEditPreview)
      : undefined;
    return message;
  },
};

function createBaseVec2(): Vec2 {
  return { x: 0, y: 0 };
}

export const Vec2: MessageFns<Vec2> = {
  encode(message: Vec2, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(13).float(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(21).float(message.y);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Vec2 {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseVec2();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 13) {
              break;
            }

            message.x = reader.float();
            continue;
          }
          case 2: {
            if (tag !== 21) {
              break;
            }

            message.y = reader.float();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Vec2 {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
    };
  },

  toJSON(message: Vec2): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = message.x;
    }
    if (message.y !== 0) {
      obj.y = message.y;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Vec2>, I>>(base?: I): Vec2 {
    return Vec2.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Vec2>, I>>(object: I): Vec2 {
    const message = createBaseVec2();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    return message;
  },
};

function createBaseJoin(): Join {
  return { roomId: "", protocolVersion: 0, definitions: [], pageHidden: false };
}

export const Join: MessageFns<Join> = {
  encode(message: Join, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.protocolVersion !== 0) {
      writer.uint32(16).uint32(message.protocolVersion);
    }
    for (const v of message.definitions) {
      DefinitionVersion.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.pageHidden !== false) {
      writer.uint32(48).bool(message.pageHidden);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Join {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseJoin();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.roomId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.protocolVersion = reader.uint32();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.definitions.push(DefinitionVersion.decode(reader, reader.uint32()));
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.pageHidden = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Join {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
      definitions: globalThis.Array.isArray(object?.definitions)
        ? object.definitions.map((e: any) => DefinitionVersion.fromJSON(e))
        : [],
      pageHidden: isSet(object.pageHidden)
        ? globalThis.Boolean(object.pageHidden)
        : isSet(object.page_hidden)
        ? globalThis.Boolean(object.page_hidden)
        : false,
    };
  },

  toJSON(message: Join): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    if (message.definitions?.length) {
      obj.definitions = message.definitions.map((e) => DefinitionVersion.toJSON(e));
    }
    if (message.pageHidden !== false) {
      obj.pageHidden = message.pageHidden;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Join>, I>>(base?: I): Join {
    return Join.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Join>, I>>(object: I): Join {
    const message = createBaseJoin();
    message.roomId = object.roomId ?? "";
    message.protocolVersion = object.protocolVersion ?? 0;
    message.definitions = object.definitions?.map((e) => DefinitionVersion.fromPartial(e)) || [];
    message.pageHidden = object.pageHidden ?? false;
    return message;
  },
};

function createBaseDefinitionVersion(): DefinitionVersion {
  return { definitionId: "", version: 0 };
}

export const DefinitionVersion: MessageFns<DefinitionVersion> = {
  encode(message: DefinitionVersion, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.definitionId !== "") {
      writer.uint32(10).string(message.definitionId);
    }
    if (message.version !== 0) {
      writer.uint32(16).uint32(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DefinitionVersion {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDefinitionVersion();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.definitionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.version = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DefinitionVersion {
    return {
      definitionId: isSet(object.definitionId)
        ? globalThis.String(object.definitionId)
        : isSet(object.definition_id)
        ? globalThis.String(object.definition_id)
        : "",
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
    };
  },

  toJSON(message: DefinitionVersion): unknown {
    const obj: any = {};
    if (message.definitionId !== "") {
      obj.definitionId = message.definitionId;
    }
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DefinitionVersion>, I>>(base?: I): DefinitionVersion {
    return DefinitionVersion.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DefinitionVersion>, I>>(object: I): DefinitionVersion {
    const message = createBaseDefinitionVersion();
    message.definitionId = object.definitionId ?? "";
    message.version = object.version ?? 0;
    return message;
  },
};

function createBaseJoinAccepted(): JoinAccepted {
  return {
    clientId: "",
    sceneRevision: 0,
    hostEpoch: 0,
    hostClientId: "",
    canvasDefinitionJson: new Uint8Array(0),
    snapshotJson: new Uint8Array(0),
    roomWasSleeping: false,
    tickRate: 0,
    userId: "",
    displayName: "",
    canvasId: "",
  };
}

export const JoinAccepted: MessageFns<JoinAccepted> = {
  encode(message: JoinAccepted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientId !== "") {
      writer.uint32(10).string(message.clientId);
    }
    if (message.sceneRevision !== 0) {
      writer.uint32(16).uint64(message.sceneRevision);
    }
    if (message.hostEpoch !== 0) {
      writer.uint32(24).uint64(message.hostEpoch);
    }
    if (message.hostClientId !== "") {
      writer.uint32(34).string(message.hostClientId);
    }
    if (message.canvasDefinitionJson.length !== 0) {
      writer.uint32(42).bytes(message.canvasDefinitionJson);
    }
    if (message.snapshotJson.length !== 0) {
      writer.uint32(50).bytes(message.snapshotJson);
    }
    if (message.roomWasSleeping !== false) {
      writer.uint32(56).bool(message.roomWasSleeping);
    }
    if (message.tickRate !== 0) {
      writer.uint32(64).uint32(message.tickRate);
    }
    if (message.userId !== "") {
      writer.uint32(74).string(message.userId);
    }
    if (message.displayName !== "") {
      writer.uint32(82).string(message.displayName);
    }
    if (message.canvasId !== "") {
      writer.uint32(90).string(message.canvasId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinAccepted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseJoinAccepted();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.clientId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.sceneRevision = longToNumber(reader.uint64());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.hostEpoch = longToNumber(reader.uint64());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.hostClientId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.canvasDefinitionJson = reader.bytes();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.snapshotJson = reader.bytes();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.roomWasSleeping = reader.bool();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.tickRate = reader.uint32();
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.userId = reader.string();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.displayName = reader.string();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.canvasId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): JoinAccepted {
    return {
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      sceneRevision: isSet(object.sceneRevision)
        ? globalThis.Number(object.sceneRevision)
        : isSet(object.scene_revision)
        ? globalThis.Number(object.scene_revision)
        : 0,
      hostEpoch: isSet(object.hostEpoch)
        ? globalThis.Number(object.hostEpoch)
        : isSet(object.host_epoch)
        ? globalThis.Number(object.host_epoch)
        : 0,
      hostClientId: isSet(object.hostClientId)
        ? globalThis.String(object.hostClientId)
        : isSet(object.host_client_id)
        ? globalThis.String(object.host_client_id)
        : "",
      canvasDefinitionJson: isSet(object.canvasDefinitionJson)
        ? bytesFromBase64(object.canvasDefinitionJson)
        : isSet(object.canvas_definition_json)
        ? bytesFromBase64(object.canvas_definition_json)
        : new Uint8Array(0),
      snapshotJson: isSet(object.snapshotJson)
        ? bytesFromBase64(object.snapshotJson)
        : isSet(object.snapshot_json)
        ? bytesFromBase64(object.snapshot_json)
        : new Uint8Array(0),
      roomWasSleeping: isSet(object.roomWasSleeping)
        ? globalThis.Boolean(object.roomWasSleeping)
        : isSet(object.room_was_sleeping)
        ? globalThis.Boolean(object.room_was_sleeping)
        : false,
      tickRate: isSet(object.tickRate)
        ? globalThis.Number(object.tickRate)
        : isSet(object.tick_rate)
        ? globalThis.Number(object.tick_rate)
        : 0,
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      canvasId: isSet(object.canvasId)
        ? globalThis.String(object.canvasId)
        : isSet(object.canvas_id)
        ? globalThis.String(object.canvas_id)
        : "",
    };
  },

  toJSON(message: JoinAccepted): unknown {
    const obj: any = {};
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.sceneRevision !== 0) {
      obj.sceneRevision = Math.round(message.sceneRevision);
    }
    if (message.hostEpoch !== 0) {
      obj.hostEpoch = Math.round(message.hostEpoch);
    }
    if (message.hostClientId !== "") {
      obj.hostClientId = message.hostClientId;
    }
    if (message.canvasDefinitionJson.length !== 0) {
      obj.canvasDefinitionJson = base64FromBytes(message.canvasDefinitionJson);
    }
    if (message.snapshotJson.length !== 0) {
      obj.snapshotJson = base64FromBytes(message.snapshotJson);
    }
    if (message.roomWasSleeping !== false) {
      obj.roomWasSleeping = message.roomWasSleeping;
    }
    if (message.tickRate !== 0) {
      obj.tickRate = Math.round(message.tickRate);
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.canvasId !== "") {
      obj.canvasId = message.canvasId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinAccepted>, I>>(base?: I): JoinAccepted {
    return JoinAccepted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinAccepted>, I>>(object: I): JoinAccepted {
    const message = createBaseJoinAccepted();
    message.clientId = object.clientId ?? "";
    message.sceneRevision = object.sceneRevision ?? 0;
    message.hostEpoch = object.hostEpoch ?? 0;
    message.hostClientId = object.hostClientId ?? "";
    message.canvasDefinitionJson = object.canvasDefinitionJson ?? new Uint8Array(0);
    message.snapshotJson = object.snapshotJson ?? new Uint8Array(0);
    message.roomWasSleeping = object.roomWasSleeping ?? false;
    message.tickRate = object.tickRate ?? 0;
    message.userId = object.userId ?? "";
    message.displayName = object.displayName ?? "";
    message.canvasId = object.canvasId ?? "";
    return message;
  },
};

function createBasePresence(): Presence {
  return { peers: [] };
}

export const Presence: MessageFns<Presence> = {
  encode(message: Presence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.peers) {
      Peer.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Presence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePresence();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.peers.push(Peer.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Presence {
    return { peers: globalThis.Array.isArray(object?.peers) ? object.peers.map((e: any) => Peer.fromJSON(e)) : [] };
  },

  toJSON(message: Presence): unknown {
    const obj: any = {};
    if (message.peers?.length) {
      obj.peers = message.peers.map((e) => Peer.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Presence>, I>>(base?: I): Presence {
    return Presence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Presence>, I>>(object: I): Presence {
    const message = createBasePresence();
    message.peers = object.peers?.map((e) => Peer.fromPartial(e)) || [];
    return message;
  },
};

function createBasePeer(): Peer {
  return { clientId: "", userId: "", displayName: "", isHost: false, hostEligible: false };
}

export const Peer: MessageFns<Peer> = {
  encode(message: Peer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientId !== "") {
      writer.uint32(10).string(message.clientId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.displayName !== "") {
      writer.uint32(26).string(message.displayName);
    }
    if (message.isHost !== false) {
      writer.uint32(32).bool(message.isHost);
    }
    if (message.hostEligible !== false) {
      writer.uint32(40).bool(message.hostEligible);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Peer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePeer();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.clientId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.userId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.displayName = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.isHost = reader.bool();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.hostEligible = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Peer {
    return {
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      isHost: isSet(object.isHost)
        ? globalThis.Boolean(object.isHost)
        : isSet(object.is_host)
        ? globalThis.Boolean(object.is_host)
        : false,
      hostEligible: isSet(object.hostEligible)
        ? globalThis.Boolean(object.hostEligible)
        : isSet(object.host_eligible)
        ? globalThis.Boolean(object.host_eligible)
        : false,
    };
  },

  toJSON(message: Peer): unknown {
    const obj: any = {};
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.isHost !== false) {
      obj.isHost = message.isHost;
    }
    if (message.hostEligible !== false) {
      obj.hostEligible = message.hostEligible;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Peer>, I>>(base?: I): Peer {
    return Peer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Peer>, I>>(object: I): Peer {
    const message = createBasePeer();
    message.clientId = object.clientId ?? "";
    message.userId = object.userId ?? "";
    message.displayName = object.displayName ?? "";
    message.isHost = object.isHost ?? false;
    message.hostEligible = object.hostEligible ?? false;
    return message;
  },
};

function createBaseHostControl(): HostControl {
  return {
    kind: 0,
    hostClientId: "",
    hostEpoch: 0,
    snapshotJson: new Uint8Array(0),
    reason: "",
    eligible: false,
    leaseExpiresAtUnixMs: 0,
  };
}

export const HostControl: MessageFns<HostControl> = {
  encode(message: HostControl, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== 0) {
      writer.uint32(8).int32(message.kind);
    }
    if (message.hostClientId !== "") {
      writer.uint32(18).string(message.hostClientId);
    }
    if (message.hostEpoch !== 0) {
      writer.uint32(24).uint64(message.hostEpoch);
    }
    if (message.snapshotJson.length !== 0) {
      writer.uint32(34).bytes(message.snapshotJson);
    }
    if (message.reason !== "") {
      writer.uint32(42).string(message.reason);
    }
    if (message.eligible !== false) {
      writer.uint32(48).bool(message.eligible);
    }
    if (message.leaseExpiresAtUnixMs !== 0) {
      writer.uint32(56).uint64(message.leaseExpiresAtUnixMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HostControl {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHostControl();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.kind = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.hostClientId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.hostEpoch = longToNumber(reader.uint64());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.snapshotJson = reader.bytes();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.reason = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.eligible = reader.bool();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.leaseExpiresAtUnixMs = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): HostControl {
    return {
      kind: isSet(object.kind) ? hostControlKindFromJSON(object.kind) : 0,
      hostClientId: isSet(object.hostClientId)
        ? globalThis.String(object.hostClientId)
        : isSet(object.host_client_id)
        ? globalThis.String(object.host_client_id)
        : "",
      hostEpoch: isSet(object.hostEpoch)
        ? globalThis.Number(object.hostEpoch)
        : isSet(object.host_epoch)
        ? globalThis.Number(object.host_epoch)
        : 0,
      snapshotJson: isSet(object.snapshotJson)
        ? bytesFromBase64(object.snapshotJson)
        : isSet(object.snapshot_json)
        ? bytesFromBase64(object.snapshot_json)
        : new Uint8Array(0),
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      eligible: isSet(object.eligible) ? globalThis.Boolean(object.eligible) : false,
      leaseExpiresAtUnixMs: isSet(object.leaseExpiresAtUnixMs)
        ? globalThis.Number(object.leaseExpiresAtUnixMs)
        : isSet(object.lease_expires_at_unix_ms)
        ? globalThis.Number(object.lease_expires_at_unix_ms)
        : 0,
    };
  },

  toJSON(message: HostControl): unknown {
    const obj: any = {};
    if (message.kind !== 0) {
      obj.kind = hostControlKindToJSON(message.kind);
    }
    if (message.hostClientId !== "") {
      obj.hostClientId = message.hostClientId;
    }
    if (message.hostEpoch !== 0) {
      obj.hostEpoch = Math.round(message.hostEpoch);
    }
    if (message.snapshotJson.length !== 0) {
      obj.snapshotJson = base64FromBytes(message.snapshotJson);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.eligible !== false) {
      obj.eligible = message.eligible;
    }
    if (message.leaseExpiresAtUnixMs !== 0) {
      obj.leaseExpiresAtUnixMs = Math.round(message.leaseExpiresAtUnixMs);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<HostControl>, I>>(base?: I): HostControl {
    return HostControl.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HostControl>, I>>(object: I): HostControl {
    const message = createBaseHostControl();
    message.kind = object.kind ?? 0;
    message.hostClientId = object.hostClientId ?? "";
    message.hostEpoch = object.hostEpoch ?? 0;
    message.snapshotJson = object.snapshotJson ?? new Uint8Array(0);
    message.reason = object.reason ?? "";
    message.eligible = object.eligible ?? false;
    message.leaseExpiresAtUnixMs = object.leaseExpiresAtUnixMs ?? 0;
    return message;
  },
};

function createBaseHeartbeat(): Heartbeat {
  return { sentAtUnixMs: 0, simulationHz: 0, workerDriftMs: 0, pageVisible: false };
}

export const Heartbeat: MessageFns<Heartbeat> = {
  encode(message: Heartbeat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sentAtUnixMs !== 0) {
      writer.uint32(8).uint64(message.sentAtUnixMs);
    }
    if (message.simulationHz !== 0) {
      writer.uint32(21).float(message.simulationHz);
    }
    if (message.workerDriftMs !== 0) {
      writer.uint32(29).float(message.workerDriftMs);
    }
    if (message.pageVisible !== false) {
      writer.uint32(32).bool(message.pageVisible);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Heartbeat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHeartbeat();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.sentAtUnixMs = longToNumber(reader.uint64());
            continue;
          }
          case 2: {
            if (tag !== 21) {
              break;
            }

            message.simulationHz = reader.float();
            continue;
          }
          case 3: {
            if (tag !== 29) {
              break;
            }

            message.workerDriftMs = reader.float();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.pageVisible = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Heartbeat {
    return {
      sentAtUnixMs: isSet(object.sentAtUnixMs)
        ? globalThis.Number(object.sentAtUnixMs)
        : isSet(object.sent_at_unix_ms)
        ? globalThis.Number(object.sent_at_unix_ms)
        : 0,
      simulationHz: isSet(object.simulationHz)
        ? globalThis.Number(object.simulationHz)
        : isSet(object.simulation_hz)
        ? globalThis.Number(object.simulation_hz)
        : 0,
      workerDriftMs: isSet(object.workerDriftMs)
        ? globalThis.Number(object.workerDriftMs)
        : isSet(object.worker_drift_ms)
        ? globalThis.Number(object.worker_drift_ms)
        : 0,
      pageVisible: isSet(object.pageVisible)
        ? globalThis.Boolean(object.pageVisible)
        : isSet(object.page_visible)
        ? globalThis.Boolean(object.page_visible)
        : false,
    };
  },

  toJSON(message: Heartbeat): unknown {
    const obj: any = {};
    if (message.sentAtUnixMs !== 0) {
      obj.sentAtUnixMs = Math.round(message.sentAtUnixMs);
    }
    if (message.simulationHz !== 0) {
      obj.simulationHz = message.simulationHz;
    }
    if (message.workerDriftMs !== 0) {
      obj.workerDriftMs = message.workerDriftMs;
    }
    if (message.pageVisible !== false) {
      obj.pageVisible = message.pageVisible;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Heartbeat>, I>>(base?: I): Heartbeat {
    return Heartbeat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Heartbeat>, I>>(object: I): Heartbeat {
    const message = createBaseHeartbeat();
    message.sentAtUnixMs = object.sentAtUnixMs ?? 0;
    message.simulationHz = object.simulationHz ?? 0;
    message.workerDriftMs = object.workerDriftMs ?? 0;
    message.pageVisible = object.pageVisible ?? false;
    return message;
  },
};

function createBasePlayerInput(): PlayerInput {
  return {
    inputSequence: 0,
    direction: undefined,
    intensity: 0,
    clientTimeUnixMs: 0,
    held: false,
    avatarDisabled: false,
    targetPosition: undefined,
  };
}

export const PlayerInput: MessageFns<PlayerInput> = {
  encode(message: PlayerInput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.inputSequence !== 0) {
      writer.uint32(8).uint32(message.inputSequence);
    }
    if (message.direction !== undefined) {
      Vec2.encode(message.direction, writer.uint32(18).fork()).join();
    }
    if (message.intensity !== 0) {
      writer.uint32(29).float(message.intensity);
    }
    if (message.clientTimeUnixMs !== 0) {
      writer.uint32(32).uint64(message.clientTimeUnixMs);
    }
    if (message.held !== false) {
      writer.uint32(40).bool(message.held);
    }
    if (message.avatarDisabled !== false) {
      writer.uint32(48).bool(message.avatarDisabled);
    }
    if (message.targetPosition !== undefined) {
      Vec2.encode(message.targetPosition, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerInput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePlayerInput();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.inputSequence = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.direction = Vec2.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 29) {
              break;
            }

            message.intensity = reader.float();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.clientTimeUnixMs = longToNumber(reader.uint64());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.held = reader.bool();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.avatarDisabled = reader.bool();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.targetPosition = Vec2.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PlayerInput {
    return {
      inputSequence: isSet(object.inputSequence)
        ? globalThis.Number(object.inputSequence)
        : isSet(object.input_sequence)
        ? globalThis.Number(object.input_sequence)
        : 0,
      direction: isSet(object.direction) ? Vec2.fromJSON(object.direction) : undefined,
      intensity: isSet(object.intensity) ? globalThis.Number(object.intensity) : 0,
      clientTimeUnixMs: isSet(object.clientTimeUnixMs)
        ? globalThis.Number(object.clientTimeUnixMs)
        : isSet(object.client_time_unix_ms)
        ? globalThis.Number(object.client_time_unix_ms)
        : 0,
      held: isSet(object.held) ? globalThis.Boolean(object.held) : false,
      avatarDisabled: isSet(object.avatarDisabled)
        ? globalThis.Boolean(object.avatarDisabled)
        : isSet(object.avatar_disabled)
        ? globalThis.Boolean(object.avatar_disabled)
        : false,
      targetPosition: isSet(object.targetPosition)
        ? Vec2.fromJSON(object.targetPosition)
        : isSet(object.target_position)
        ? Vec2.fromJSON(object.target_position)
        : undefined,
    };
  },

  toJSON(message: PlayerInput): unknown {
    const obj: any = {};
    if (message.inputSequence !== 0) {
      obj.inputSequence = Math.round(message.inputSequence);
    }
    if (message.direction !== undefined) {
      obj.direction = Vec2.toJSON(message.direction);
    }
    if (message.intensity !== 0) {
      obj.intensity = message.intensity;
    }
    if (message.clientTimeUnixMs !== 0) {
      obj.clientTimeUnixMs = Math.round(message.clientTimeUnixMs);
    }
    if (message.held !== false) {
      obj.held = message.held;
    }
    if (message.avatarDisabled !== false) {
      obj.avatarDisabled = message.avatarDisabled;
    }
    if (message.targetPosition !== undefined) {
      obj.targetPosition = Vec2.toJSON(message.targetPosition);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerInput>, I>>(base?: I): PlayerInput {
    return PlayerInput.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerInput>, I>>(object: I): PlayerInput {
    const message = createBasePlayerInput();
    message.inputSequence = object.inputSequence ?? 0;
    message.direction = (object.direction !== undefined && object.direction !== null)
      ? Vec2.fromPartial(object.direction)
      : undefined;
    message.intensity = object.intensity ?? 0;
    message.clientTimeUnixMs = object.clientTimeUnixMs ?? 0;
    message.held = object.held ?? false;
    message.avatarDisabled = object.avatarDisabled ?? false;
    message.targetPosition = (object.targetPosition !== undefined && object.targetPosition !== null)
      ? Vec2.fromPartial(object.targetPosition)
      : undefined;
    return message;
  },
};

function createBaseEntityState(): EntityState {
  return {
    entityId: "",
    quantizedTransform: undefined,
    lastProcessedInputSequence: 0,
    spriteVariant: "",
    behaviorStateJson: new Uint8Array(0),
    quarantined: false,
    definitionId: "",
    disabled: false,
    teleportEpoch: 0,
    respawning: false,
    spriteAnimation: "",
    animationEpoch: 0,
    itemIsolated: false,
    spriteTint: 0,
    hasSpriteTint: false,
    itemCollisionsDisabled: false,
  };
}

export const EntityState: MessageFns<EntityState> = {
  encode(message: EntityState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entityId !== "") {
      writer.uint32(10).string(message.entityId);
    }
    if (message.quantizedTransform !== undefined) {
      QuantizedTransform.encode(message.quantizedTransform, writer.uint32(18).fork()).join();
    }
    if (message.lastProcessedInputSequence !== 0) {
      writer.uint32(64).uint32(message.lastProcessedInputSequence);
    }
    if (message.spriteVariant !== "") {
      writer.uint32(74).string(message.spriteVariant);
    }
    if (message.behaviorStateJson.length !== 0) {
      writer.uint32(82).bytes(message.behaviorStateJson);
    }
    if (message.quarantined !== false) {
      writer.uint32(88).bool(message.quarantined);
    }
    if (message.definitionId !== "") {
      writer.uint32(98).string(message.definitionId);
    }
    if (message.disabled !== false) {
      writer.uint32(104).bool(message.disabled);
    }
    if (message.teleportEpoch !== 0) {
      writer.uint32(112).uint32(message.teleportEpoch);
    }
    if (message.respawning !== false) {
      writer.uint32(120).bool(message.respawning);
    }
    if (message.spriteAnimation !== "") {
      writer.uint32(130).string(message.spriteAnimation);
    }
    if (message.animationEpoch !== 0) {
      writer.uint32(136).uint32(message.animationEpoch);
    }
    if (message.itemIsolated !== false) {
      writer.uint32(144).bool(message.itemIsolated);
    }
    if (message.spriteTint !== 0) {
      writer.uint32(152).uint32(message.spriteTint);
    }
    if (message.hasSpriteTint !== false) {
      writer.uint32(160).bool(message.hasSpriteTint);
    }
    if (message.itemCollisionsDisabled !== false) {
      writer.uint32(168).bool(message.itemCollisionsDisabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EntityState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEntityState();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.quantizedTransform = QuantizedTransform.decode(reader, reader.uint32());
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.lastProcessedInputSequence = reader.uint32();
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.spriteVariant = reader.string();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.behaviorStateJson = reader.bytes();
            continue;
          }
          case 11: {
            if (tag !== 88) {
              break;
            }

            message.quarantined = reader.bool();
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.definitionId = reader.string();
            continue;
          }
          case 13: {
            if (tag !== 104) {
              break;
            }

            message.disabled = reader.bool();
            continue;
          }
          case 14: {
            if (tag !== 112) {
              break;
            }

            message.teleportEpoch = reader.uint32();
            continue;
          }
          case 15: {
            if (tag !== 120) {
              break;
            }

            message.respawning = reader.bool();
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.spriteAnimation = reader.string();
            continue;
          }
          case 17: {
            if (tag !== 136) {
              break;
            }

            message.animationEpoch = reader.uint32();
            continue;
          }
          case 18: {
            if (tag !== 144) {
              break;
            }

            message.itemIsolated = reader.bool();
            continue;
          }
          case 19: {
            if (tag !== 152) {
              break;
            }

            message.spriteTint = reader.uint32();
            continue;
          }
          case 20: {
            if (tag !== 160) {
              break;
            }

            message.hasSpriteTint = reader.bool();
            continue;
          }
          case 21: {
            if (tag !== 168) {
              break;
            }

            message.itemCollisionsDisabled = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EntityState {
    return {
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
      quantizedTransform: isSet(object.quantizedTransform)
        ? QuantizedTransform.fromJSON(object.quantizedTransform)
        : isSet(object.quantized_transform)
        ? QuantizedTransform.fromJSON(object.quantized_transform)
        : undefined,
      lastProcessedInputSequence: isSet(object.lastProcessedInputSequence)
        ? globalThis.Number(object.lastProcessedInputSequence)
        : isSet(object.last_processed_input_sequence)
        ? globalThis.Number(object.last_processed_input_sequence)
        : 0,
      spriteVariant: isSet(object.spriteVariant)
        ? globalThis.String(object.spriteVariant)
        : isSet(object.sprite_variant)
        ? globalThis.String(object.sprite_variant)
        : "",
      behaviorStateJson: isSet(object.behaviorStateJson)
        ? bytesFromBase64(object.behaviorStateJson)
        : isSet(object.behavior_state_json)
        ? bytesFromBase64(object.behavior_state_json)
        : new Uint8Array(0),
      quarantined: isSet(object.quarantined) ? globalThis.Boolean(object.quarantined) : false,
      definitionId: isSet(object.definitionId)
        ? globalThis.String(object.definitionId)
        : isSet(object.definition_id)
        ? globalThis.String(object.definition_id)
        : "",
      disabled: isSet(object.disabled) ? globalThis.Boolean(object.disabled) : false,
      teleportEpoch: isSet(object.teleportEpoch)
        ? globalThis.Number(object.teleportEpoch)
        : isSet(object.teleport_epoch)
        ? globalThis.Number(object.teleport_epoch)
        : 0,
      respawning: isSet(object.respawning) ? globalThis.Boolean(object.respawning) : false,
      spriteAnimation: isSet(object.spriteAnimation)
        ? globalThis.String(object.spriteAnimation)
        : isSet(object.sprite_animation)
        ? globalThis.String(object.sprite_animation)
        : "",
      animationEpoch: isSet(object.animationEpoch)
        ? globalThis.Number(object.animationEpoch)
        : isSet(object.animation_epoch)
        ? globalThis.Number(object.animation_epoch)
        : 0,
      itemIsolated: isSet(object.itemIsolated)
        ? globalThis.Boolean(object.itemIsolated)
        : isSet(object.item_isolated)
        ? globalThis.Boolean(object.item_isolated)
        : false,
      spriteTint: isSet(object.spriteTint)
        ? globalThis.Number(object.spriteTint)
        : isSet(object.sprite_tint)
        ? globalThis.Number(object.sprite_tint)
        : 0,
      hasSpriteTint: isSet(object.hasSpriteTint)
        ? globalThis.Boolean(object.hasSpriteTint)
        : isSet(object.has_sprite_tint)
        ? globalThis.Boolean(object.has_sprite_tint)
        : false,
      itemCollisionsDisabled: isSet(object.itemCollisionsDisabled)
        ? globalThis.Boolean(object.itemCollisionsDisabled)
        : isSet(object.item_collisions_disabled)
        ? globalThis.Boolean(object.item_collisions_disabled)
        : false,
    };
  },

  toJSON(message: EntityState): unknown {
    const obj: any = {};
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    if (message.quantizedTransform !== undefined) {
      obj.quantizedTransform = QuantizedTransform.toJSON(message.quantizedTransform);
    }
    if (message.lastProcessedInputSequence !== 0) {
      obj.lastProcessedInputSequence = Math.round(message.lastProcessedInputSequence);
    }
    if (message.spriteVariant !== "") {
      obj.spriteVariant = message.spriteVariant;
    }
    if (message.behaviorStateJson.length !== 0) {
      obj.behaviorStateJson = base64FromBytes(message.behaviorStateJson);
    }
    if (message.quarantined !== false) {
      obj.quarantined = message.quarantined;
    }
    if (message.definitionId !== "") {
      obj.definitionId = message.definitionId;
    }
    if (message.disabled !== false) {
      obj.disabled = message.disabled;
    }
    if (message.teleportEpoch !== 0) {
      obj.teleportEpoch = Math.round(message.teleportEpoch);
    }
    if (message.respawning !== false) {
      obj.respawning = message.respawning;
    }
    if (message.spriteAnimation !== "") {
      obj.spriteAnimation = message.spriteAnimation;
    }
    if (message.animationEpoch !== 0) {
      obj.animationEpoch = Math.round(message.animationEpoch);
    }
    if (message.itemIsolated !== false) {
      obj.itemIsolated = message.itemIsolated;
    }
    if (message.spriteTint !== 0) {
      obj.spriteTint = Math.round(message.spriteTint);
    }
    if (message.hasSpriteTint !== false) {
      obj.hasSpriteTint = message.hasSpriteTint;
    }
    if (message.itemCollisionsDisabled !== false) {
      obj.itemCollisionsDisabled = message.itemCollisionsDisabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EntityState>, I>>(base?: I): EntityState {
    return EntityState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EntityState>, I>>(object: I): EntityState {
    const message = createBaseEntityState();
    message.entityId = object.entityId ?? "";
    message.quantizedTransform = (object.quantizedTransform !== undefined && object.quantizedTransform !== null)
      ? QuantizedTransform.fromPartial(object.quantizedTransform)
      : undefined;
    message.lastProcessedInputSequence = object.lastProcessedInputSequence ?? 0;
    message.spriteVariant = object.spriteVariant ?? "";
    message.behaviorStateJson = object.behaviorStateJson ?? new Uint8Array(0);
    message.quarantined = object.quarantined ?? false;
    message.definitionId = object.definitionId ?? "";
    message.disabled = object.disabled ?? false;
    message.teleportEpoch = object.teleportEpoch ?? 0;
    message.respawning = object.respawning ?? false;
    message.spriteAnimation = object.spriteAnimation ?? "";
    message.animationEpoch = object.animationEpoch ?? 0;
    message.itemIsolated = object.itemIsolated ?? false;
    message.spriteTint = object.spriteTint ?? 0;
    message.hasSpriteTint = object.hasSpriteTint ?? false;
    message.itemCollisionsDisabled = object.itemCollisionsDisabled ?? false;
    return message;
  },
};

function createBaseQuantizedTransform(): QuantizedTransform {
  return { x: 0, y: 0, rotation: 0, vx: 0, vy: 0, angularVelocity: 0, z: 0, vz: 0, scale: 0 };
}

export const QuantizedTransform: MessageFns<QuantizedTransform> = {
  encode(message: QuantizedTransform, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.x !== 0) {
      writer.uint32(8).sint32(message.x);
    }
    if (message.y !== 0) {
      writer.uint32(16).sint32(message.y);
    }
    if (message.rotation !== 0) {
      writer.uint32(24).sint32(message.rotation);
    }
    if (message.vx !== 0) {
      writer.uint32(32).sint32(message.vx);
    }
    if (message.vy !== 0) {
      writer.uint32(40).sint32(message.vy);
    }
    if (message.angularVelocity !== 0) {
      writer.uint32(48).sint32(message.angularVelocity);
    }
    if (message.z !== 0) {
      writer.uint32(56).sint32(message.z);
    }
    if (message.vz !== 0) {
      writer.uint32(64).sint32(message.vz);
    }
    if (message.scale !== 0) {
      writer.uint32(72).sint32(message.scale);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QuantizedTransform {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQuantizedTransform();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.x = reader.sint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.y = reader.sint32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.rotation = reader.sint32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.vx = reader.sint32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.vy = reader.sint32();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.angularVelocity = reader.sint32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.z = reader.sint32();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.vz = reader.sint32();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.scale = reader.sint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QuantizedTransform {
    return {
      x: isSet(object.x) ? globalThis.Number(object.x) : 0,
      y: isSet(object.y) ? globalThis.Number(object.y) : 0,
      rotation: isSet(object.rotation) ? globalThis.Number(object.rotation) : 0,
      vx: isSet(object.vx) ? globalThis.Number(object.vx) : 0,
      vy: isSet(object.vy) ? globalThis.Number(object.vy) : 0,
      angularVelocity: isSet(object.angularVelocity)
        ? globalThis.Number(object.angularVelocity)
        : isSet(object.angular_velocity)
        ? globalThis.Number(object.angular_velocity)
        : 0,
      z: isSet(object.z) ? globalThis.Number(object.z) : 0,
      vz: isSet(object.vz) ? globalThis.Number(object.vz) : 0,
      scale: isSet(object.scale) ? globalThis.Number(object.scale) : 0,
    };
  },

  toJSON(message: QuantizedTransform): unknown {
    const obj: any = {};
    if (message.x !== 0) {
      obj.x = Math.round(message.x);
    }
    if (message.y !== 0) {
      obj.y = Math.round(message.y);
    }
    if (message.rotation !== 0) {
      obj.rotation = Math.round(message.rotation);
    }
    if (message.vx !== 0) {
      obj.vx = Math.round(message.vx);
    }
    if (message.vy !== 0) {
      obj.vy = Math.round(message.vy);
    }
    if (message.angularVelocity !== 0) {
      obj.angularVelocity = Math.round(message.angularVelocity);
    }
    if (message.z !== 0) {
      obj.z = Math.round(message.z);
    }
    if (message.vz !== 0) {
      obj.vz = Math.round(message.vz);
    }
    if (message.scale !== 0) {
      obj.scale = Math.round(message.scale);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QuantizedTransform>, I>>(base?: I): QuantizedTransform {
    return QuantizedTransform.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QuantizedTransform>, I>>(object: I): QuantizedTransform {
    const message = createBaseQuantizedTransform();
    message.x = object.x ?? 0;
    message.y = object.y ?? 0;
    message.rotation = object.rotation ?? 0;
    message.vx = object.vx ?? 0;
    message.vy = object.vy ?? 0;
    message.angularVelocity = object.angularVelocity ?? 0;
    message.z = object.z ?? 0;
    message.vz = object.vz ?? 0;
    message.scale = object.scale ?? 0;
    return message;
  },
};

function createBaseStateDelta(): StateDelta {
  return { entities: [], removedEntityIds: [], sceneRevision: 0 };
}

export const StateDelta: MessageFns<StateDelta> = {
  encode(message: StateDelta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entities) {
      EntityState.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.removedEntityIds) {
      writer.uint32(18).string(v!);
    }
    if (message.sceneRevision !== 0) {
      writer.uint32(24).uint64(message.sceneRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StateDelta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseStateDelta();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.entities.push(EntityState.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.removedEntityIds.push(reader.string());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.sceneRevision = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): StateDelta {
    return {
      entities: globalThis.Array.isArray(object?.entities)
        ? object.entities.map((e: any) => EntityState.fromJSON(e))
        : [],
      removedEntityIds: globalThis.Array.isArray(object?.removedEntityIds)
        ? object.removedEntityIds.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.removed_entity_ids)
        ? object.removed_entity_ids.map((e: any) => globalThis.String(e))
        : [],
      sceneRevision: isSet(object.sceneRevision)
        ? globalThis.Number(object.sceneRevision)
        : isSet(object.scene_revision)
        ? globalThis.Number(object.scene_revision)
        : 0,
    };
  },

  toJSON(message: StateDelta): unknown {
    const obj: any = {};
    if (message.entities?.length) {
      obj.entities = message.entities.map((e) => EntityState.toJSON(e));
    }
    if (message.removedEntityIds?.length) {
      obj.removedEntityIds = message.removedEntityIds;
    }
    if (message.sceneRevision !== 0) {
      obj.sceneRevision = Math.round(message.sceneRevision);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StateDelta>, I>>(base?: I): StateDelta {
    return StateDelta.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StateDelta>, I>>(object: I): StateDelta {
    const message = createBaseStateDelta();
    message.entities = object.entities?.map((e) => EntityState.fromPartial(e)) || [];
    message.removedEntityIds = object.removedEntityIds?.map((e) => e) || [];
    message.sceneRevision = object.sceneRevision ?? 0;
    return message;
  },
};

function createBaseFullState(): FullState {
  return { entities: [], avatars: [], sceneRevision: 0, tickRate: 0 };
}

export const FullState: MessageFns<FullState> = {
  encode(message: FullState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entities) {
      EntityState.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.avatars) {
      AvatarPresence.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.sceneRevision !== 0) {
      writer.uint32(24).uint64(message.sceneRevision);
    }
    if (message.tickRate !== 0) {
      writer.uint32(32).uint32(message.tickRate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FullState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFullState();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.entities.push(EntityState.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.avatars.push(AvatarPresence.decode(reader, reader.uint32()));
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.sceneRevision = longToNumber(reader.uint64());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.tickRate = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): FullState {
    return {
      entities: globalThis.Array.isArray(object?.entities)
        ? object.entities.map((e: any) => EntityState.fromJSON(e))
        : [],
      avatars: globalThis.Array.isArray(object?.avatars)
        ? object.avatars.map((e: any) => AvatarPresence.fromJSON(e))
        : [],
      sceneRevision: isSet(object.sceneRevision)
        ? globalThis.Number(object.sceneRevision)
        : isSet(object.scene_revision)
        ? globalThis.Number(object.scene_revision)
        : 0,
      tickRate: isSet(object.tickRate)
        ? globalThis.Number(object.tickRate)
        : isSet(object.tick_rate)
        ? globalThis.Number(object.tick_rate)
        : 0,
    };
  },

  toJSON(message: FullState): unknown {
    const obj: any = {};
    if (message.entities?.length) {
      obj.entities = message.entities.map((e) => EntityState.toJSON(e));
    }
    if (message.avatars?.length) {
      obj.avatars = message.avatars.map((e) => AvatarPresence.toJSON(e));
    }
    if (message.sceneRevision !== 0) {
      obj.sceneRevision = Math.round(message.sceneRevision);
    }
    if (message.tickRate !== 0) {
      obj.tickRate = Math.round(message.tickRate);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FullState>, I>>(base?: I): FullState {
    return FullState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FullState>, I>>(object: I): FullState {
    const message = createBaseFullState();
    message.entities = object.entities?.map((e) => EntityState.fromPartial(e)) || [];
    message.avatars = object.avatars?.map((e) => AvatarPresence.fromPartial(e)) || [];
    message.sceneRevision = object.sceneRevision ?? 0;
    message.tickRate = object.tickRate ?? 0;
    return message;
  },
};

function createBaseAvatarPresence(): AvatarPresence {
  return { entityId: "", clientId: "", userId: "", displayName: "" };
}

export const AvatarPresence: MessageFns<AvatarPresence> = {
  encode(message: AvatarPresence, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entityId !== "") {
      writer.uint32(10).string(message.entityId);
    }
    if (message.clientId !== "") {
      writer.uint32(18).string(message.clientId);
    }
    if (message.userId !== "") {
      writer.uint32(26).string(message.userId);
    }
    if (message.displayName !== "") {
      writer.uint32(34).string(message.displayName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AvatarPresence {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAvatarPresence();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.clientId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.userId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.displayName = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AvatarPresence {
    return {
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
      clientId: isSet(object.clientId)
        ? globalThis.String(object.clientId)
        : isSet(object.client_id)
        ? globalThis.String(object.client_id)
        : "",
      userId: isSet(object.userId)
        ? globalThis.String(object.userId)
        : isSet(object.user_id)
        ? globalThis.String(object.user_id)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
    };
  },

  toJSON(message: AvatarPresence): unknown {
    const obj: any = {};
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    if (message.clientId !== "") {
      obj.clientId = message.clientId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AvatarPresence>, I>>(base?: I): AvatarPresence {
    return AvatarPresence.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AvatarPresence>, I>>(object: I): AvatarPresence {
    const message = createBaseAvatarPresence();
    message.entityId = object.entityId ?? "";
    message.clientId = object.clientId ?? "";
    message.userId = object.userId ?? "";
    message.displayName = object.displayName ?? "";
    return message;
  },
};

function createBaseEffectEvent(): EffectEvent {
  return { entityId: "", effect: "", mode: "", paramsJson: new Uint8Array(0) };
}

export const EffectEvent: MessageFns<EffectEvent> = {
  encode(message: EffectEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.entityId !== "") {
      writer.uint32(10).string(message.entityId);
    }
    if (message.effect !== "") {
      writer.uint32(18).string(message.effect);
    }
    if (message.mode !== "") {
      writer.uint32(26).string(message.mode);
    }
    if (message.paramsJson.length !== 0) {
      writer.uint32(34).bytes(message.paramsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EffectEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEffectEvent();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.effect = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.mode = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.paramsJson = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EffectEvent {
    return {
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
      effect: isSet(object.effect) ? globalThis.String(object.effect) : "",
      mode: isSet(object.mode) ? globalThis.String(object.mode) : "",
      paramsJson: isSet(object.paramsJson)
        ? bytesFromBase64(object.paramsJson)
        : isSet(object.params_json)
        ? bytesFromBase64(object.params_json)
        : new Uint8Array(0),
    };
  },

  toJSON(message: EffectEvent): unknown {
    const obj: any = {};
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    if (message.effect !== "") {
      obj.effect = message.effect;
    }
    if (message.mode !== "") {
      obj.mode = message.mode;
    }
    if (message.paramsJson.length !== 0) {
      obj.paramsJson = base64FromBytes(message.paramsJson);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EffectEvent>, I>>(base?: I): EffectEvent {
    return EffectEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EffectEvent>, I>>(object: I): EffectEvent {
    const message = createBaseEffectEvent();
    message.entityId = object.entityId ?? "";
    message.effect = object.effect ?? "";
    message.mode = object.mode ?? "";
    message.paramsJson = object.paramsJson ?? new Uint8Array(0);
    return message;
  },
};

function createBaseItemMutation(): ItemMutation {
  return {
    clientSessionId: "",
    mutationId: 0,
    editSessionId: "",
    expectedItemRevision: 0,
    kind: 0,
    entityId: "",
    definitionId: "",
    definitionVersion: 0,
    position: undefined,
    rotation: 0,
    z: 0,
    scale: 0,
    configJson: new Uint8Array(0),
    isolated: false,
    collisionsEnabled: false,
  };
}

export const ItemMutation: MessageFns<ItemMutation> = {
  encode(message: ItemMutation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientSessionId !== "") {
      writer.uint32(10).string(message.clientSessionId);
    }
    if (message.mutationId !== 0) {
      writer.uint32(16).uint64(message.mutationId);
    }
    if (message.editSessionId !== "") {
      writer.uint32(26).string(message.editSessionId);
    }
    if (message.expectedItemRevision !== 0) {
      writer.uint32(32).uint64(message.expectedItemRevision);
    }
    if (message.kind !== 0) {
      writer.uint32(40).int32(message.kind);
    }
    if (message.entityId !== "") {
      writer.uint32(50).string(message.entityId);
    }
    if (message.definitionId !== "") {
      writer.uint32(58).string(message.definitionId);
    }
    if (message.definitionVersion !== 0) {
      writer.uint32(64).uint32(message.definitionVersion);
    }
    if (message.position !== undefined) {
      Vec2.encode(message.position, writer.uint32(74).fork()).join();
    }
    if (message.rotation !== 0) {
      writer.uint32(85).float(message.rotation);
    }
    if (message.z !== 0) {
      writer.uint32(93).float(message.z);
    }
    if (message.scale !== 0) {
      writer.uint32(101).float(message.scale);
    }
    if (message.configJson.length !== 0) {
      writer.uint32(106).bytes(message.configJson);
    }
    if (message.isolated !== false) {
      writer.uint32(112).bool(message.isolated);
    }
    if (message.collisionsEnabled !== false) {
      writer.uint32(120).bool(message.collisionsEnabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ItemMutation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseItemMutation();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.clientSessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.mutationId = longToNumber(reader.uint64());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.editSessionId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.expectedItemRevision = longToNumber(reader.uint64());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.kind = reader.int32() as any;
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.definitionId = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.definitionVersion = reader.uint32();
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.position = Vec2.decode(reader, reader.uint32());
            continue;
          }
          case 10: {
            if (tag !== 85) {
              break;
            }

            message.rotation = reader.float();
            continue;
          }
          case 11: {
            if (tag !== 93) {
              break;
            }

            message.z = reader.float();
            continue;
          }
          case 12: {
            if (tag !== 101) {
              break;
            }

            message.scale = reader.float();
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.configJson = reader.bytes();
            continue;
          }
          case 14: {
            if (tag !== 112) {
              break;
            }

            message.isolated = reader.bool();
            continue;
          }
          case 15: {
            if (tag !== 120) {
              break;
            }

            message.collisionsEnabled = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ItemMutation {
    return {
      clientSessionId: isSet(object.clientSessionId)
        ? globalThis.String(object.clientSessionId)
        : isSet(object.client_session_id)
        ? globalThis.String(object.client_session_id)
        : "",
      mutationId: isSet(object.mutationId)
        ? globalThis.Number(object.mutationId)
        : isSet(object.mutation_id)
        ? globalThis.Number(object.mutation_id)
        : 0,
      editSessionId: isSet(object.editSessionId)
        ? globalThis.String(object.editSessionId)
        : isSet(object.edit_session_id)
        ? globalThis.String(object.edit_session_id)
        : "",
      expectedItemRevision: isSet(object.expectedItemRevision)
        ? globalThis.Number(object.expectedItemRevision)
        : isSet(object.expected_item_revision)
        ? globalThis.Number(object.expected_item_revision)
        : 0,
      kind: isSet(object.kind) ? itemMutationKindFromJSON(object.kind) : 0,
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
      definitionId: isSet(object.definitionId)
        ? globalThis.String(object.definitionId)
        : isSet(object.definition_id)
        ? globalThis.String(object.definition_id)
        : "",
      definitionVersion: isSet(object.definitionVersion)
        ? globalThis.Number(object.definitionVersion)
        : isSet(object.definition_version)
        ? globalThis.Number(object.definition_version)
        : 0,
      position: isSet(object.position) ? Vec2.fromJSON(object.position) : undefined,
      rotation: isSet(object.rotation) ? globalThis.Number(object.rotation) : 0,
      z: isSet(object.z) ? globalThis.Number(object.z) : 0,
      scale: isSet(object.scale) ? globalThis.Number(object.scale) : 0,
      configJson: isSet(object.configJson)
        ? bytesFromBase64(object.configJson)
        : isSet(object.config_json)
        ? bytesFromBase64(object.config_json)
        : new Uint8Array(0),
      isolated: isSet(object.isolated) ? globalThis.Boolean(object.isolated) : false,
      collisionsEnabled: isSet(object.collisionsEnabled)
        ? globalThis.Boolean(object.collisionsEnabled)
        : isSet(object.collisions_enabled)
        ? globalThis.Boolean(object.collisions_enabled)
        : false,
    };
  },

  toJSON(message: ItemMutation): unknown {
    const obj: any = {};
    if (message.clientSessionId !== "") {
      obj.clientSessionId = message.clientSessionId;
    }
    if (message.mutationId !== 0) {
      obj.mutationId = Math.round(message.mutationId);
    }
    if (message.editSessionId !== "") {
      obj.editSessionId = message.editSessionId;
    }
    if (message.expectedItemRevision !== 0) {
      obj.expectedItemRevision = Math.round(message.expectedItemRevision);
    }
    if (message.kind !== 0) {
      obj.kind = itemMutationKindToJSON(message.kind);
    }
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    if (message.definitionId !== "") {
      obj.definitionId = message.definitionId;
    }
    if (message.definitionVersion !== 0) {
      obj.definitionVersion = Math.round(message.definitionVersion);
    }
    if (message.position !== undefined) {
      obj.position = Vec2.toJSON(message.position);
    }
    if (message.rotation !== 0) {
      obj.rotation = message.rotation;
    }
    if (message.z !== 0) {
      obj.z = message.z;
    }
    if (message.scale !== 0) {
      obj.scale = message.scale;
    }
    if (message.configJson.length !== 0) {
      obj.configJson = base64FromBytes(message.configJson);
    }
    if (message.isolated !== false) {
      obj.isolated = message.isolated;
    }
    if (message.collisionsEnabled !== false) {
      obj.collisionsEnabled = message.collisionsEnabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ItemMutation>, I>>(base?: I): ItemMutation {
    return ItemMutation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ItemMutation>, I>>(object: I): ItemMutation {
    const message = createBaseItemMutation();
    message.clientSessionId = object.clientSessionId ?? "";
    message.mutationId = object.mutationId ?? 0;
    message.editSessionId = object.editSessionId ?? "";
    message.expectedItemRevision = object.expectedItemRevision ?? 0;
    message.kind = object.kind ?? 0;
    message.entityId = object.entityId ?? "";
    message.definitionId = object.definitionId ?? "";
    message.definitionVersion = object.definitionVersion ?? 0;
    message.position = (object.position !== undefined && object.position !== null)
      ? Vec2.fromPartial(object.position)
      : undefined;
    message.rotation = object.rotation ?? 0;
    message.z = object.z ?? 0;
    message.scale = object.scale ?? 0;
    message.configJson = object.configJson ?? new Uint8Array(0);
    message.isolated = object.isolated ?? false;
    message.collisionsEnabled = object.collisionsEnabled ?? false;
    return message;
  },
};

function createBaseItemMutationResult(): ItemMutationResult {
  return {
    clientSessionId: "",
    mutationId: 0,
    editSessionId: "",
    accepted: false,
    rejectCode: 0,
    message: "",
    sceneRevision: 0,
    itemRevision: 0,
    itemInstanceJson: new Uint8Array(0),
    deletedEntityId: "",
    kind: 0,
    entityId: "",
  };
}

export const ItemMutationResult: MessageFns<ItemMutationResult> = {
  encode(message: ItemMutationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientSessionId !== "") {
      writer.uint32(10).string(message.clientSessionId);
    }
    if (message.mutationId !== 0) {
      writer.uint32(16).uint64(message.mutationId);
    }
    if (message.editSessionId !== "") {
      writer.uint32(26).string(message.editSessionId);
    }
    if (message.accepted !== false) {
      writer.uint32(32).bool(message.accepted);
    }
    if (message.rejectCode !== 0) {
      writer.uint32(40).int32(message.rejectCode);
    }
    if (message.message !== "") {
      writer.uint32(50).string(message.message);
    }
    if (message.sceneRevision !== 0) {
      writer.uint32(56).uint64(message.sceneRevision);
    }
    if (message.itemRevision !== 0) {
      writer.uint32(64).uint64(message.itemRevision);
    }
    if (message.itemInstanceJson.length !== 0) {
      writer.uint32(74).bytes(message.itemInstanceJson);
    }
    if (message.deletedEntityId !== "") {
      writer.uint32(82).string(message.deletedEntityId);
    }
    if (message.kind !== 0) {
      writer.uint32(88).int32(message.kind);
    }
    if (message.entityId !== "") {
      writer.uint32(98).string(message.entityId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ItemMutationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseItemMutationResult();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.clientSessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.mutationId = longToNumber(reader.uint64());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.editSessionId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.accepted = reader.bool();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.rejectCode = reader.int32() as any;
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.message = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.sceneRevision = longToNumber(reader.uint64());
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.itemRevision = longToNumber(reader.uint64());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.itemInstanceJson = reader.bytes();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.deletedEntityId = reader.string();
            continue;
          }
          case 11: {
            if (tag !== 88) {
              break;
            }

            message.kind = reader.int32() as any;
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ItemMutationResult {
    return {
      clientSessionId: isSet(object.clientSessionId)
        ? globalThis.String(object.clientSessionId)
        : isSet(object.client_session_id)
        ? globalThis.String(object.client_session_id)
        : "",
      mutationId: isSet(object.mutationId)
        ? globalThis.Number(object.mutationId)
        : isSet(object.mutation_id)
        ? globalThis.Number(object.mutation_id)
        : 0,
      editSessionId: isSet(object.editSessionId)
        ? globalThis.String(object.editSessionId)
        : isSet(object.edit_session_id)
        ? globalThis.String(object.edit_session_id)
        : "",
      accepted: isSet(object.accepted) ? globalThis.Boolean(object.accepted) : false,
      rejectCode: isSet(object.rejectCode)
        ? itemMutationRejectCodeFromJSON(object.rejectCode)
        : isSet(object.reject_code)
        ? itemMutationRejectCodeFromJSON(object.reject_code)
        : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      sceneRevision: isSet(object.sceneRevision)
        ? globalThis.Number(object.sceneRevision)
        : isSet(object.scene_revision)
        ? globalThis.Number(object.scene_revision)
        : 0,
      itemRevision: isSet(object.itemRevision)
        ? globalThis.Number(object.itemRevision)
        : isSet(object.item_revision)
        ? globalThis.Number(object.item_revision)
        : 0,
      itemInstanceJson: isSet(object.itemInstanceJson)
        ? bytesFromBase64(object.itemInstanceJson)
        : isSet(object.item_instance_json)
        ? bytesFromBase64(object.item_instance_json)
        : new Uint8Array(0),
      deletedEntityId: isSet(object.deletedEntityId)
        ? globalThis.String(object.deletedEntityId)
        : isSet(object.deleted_entity_id)
        ? globalThis.String(object.deleted_entity_id)
        : "",
      kind: isSet(object.kind) ? itemMutationKindFromJSON(object.kind) : 0,
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
    };
  },

  toJSON(message: ItemMutationResult): unknown {
    const obj: any = {};
    if (message.clientSessionId !== "") {
      obj.clientSessionId = message.clientSessionId;
    }
    if (message.mutationId !== 0) {
      obj.mutationId = Math.round(message.mutationId);
    }
    if (message.editSessionId !== "") {
      obj.editSessionId = message.editSessionId;
    }
    if (message.accepted !== false) {
      obj.accepted = message.accepted;
    }
    if (message.rejectCode !== 0) {
      obj.rejectCode = itemMutationRejectCodeToJSON(message.rejectCode);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.sceneRevision !== 0) {
      obj.sceneRevision = Math.round(message.sceneRevision);
    }
    if (message.itemRevision !== 0) {
      obj.itemRevision = Math.round(message.itemRevision);
    }
    if (message.itemInstanceJson.length !== 0) {
      obj.itemInstanceJson = base64FromBytes(message.itemInstanceJson);
    }
    if (message.deletedEntityId !== "") {
      obj.deletedEntityId = message.deletedEntityId;
    }
    if (message.kind !== 0) {
      obj.kind = itemMutationKindToJSON(message.kind);
    }
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ItemMutationResult>, I>>(base?: I): ItemMutationResult {
    return ItemMutationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ItemMutationResult>, I>>(object: I): ItemMutationResult {
    const message = createBaseItemMutationResult();
    message.clientSessionId = object.clientSessionId ?? "";
    message.mutationId = object.mutationId ?? 0;
    message.editSessionId = object.editSessionId ?? "";
    message.accepted = object.accepted ?? false;
    message.rejectCode = object.rejectCode ?? 0;
    message.message = object.message ?? "";
    message.sceneRevision = object.sceneRevision ?? 0;
    message.itemRevision = object.itemRevision ?? 0;
    message.itemInstanceJson = object.itemInstanceJson ?? new Uint8Array(0);
    message.deletedEntityId = object.deletedEntityId ?? "";
    message.kind = object.kind ?? 0;
    message.entityId = object.entityId ?? "";
    return message;
  },
};

function createBaseBeginItemEdit(): BeginItemEdit {
  return { clientSessionId: "", editSessionId: "", entityId: "", observedItemRevision: 0 };
}

export const BeginItemEdit: MessageFns<BeginItemEdit> = {
  encode(message: BeginItemEdit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientSessionId !== "") {
      writer.uint32(10).string(message.clientSessionId);
    }
    if (message.editSessionId !== "") {
      writer.uint32(18).string(message.editSessionId);
    }
    if (message.entityId !== "") {
      writer.uint32(26).string(message.entityId);
    }
    if (message.observedItemRevision !== 0) {
      writer.uint32(32).uint64(message.observedItemRevision);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BeginItemEdit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBeginItemEdit();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.clientSessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.editSessionId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.observedItemRevision = longToNumber(reader.uint64());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): BeginItemEdit {
    return {
      clientSessionId: isSet(object.clientSessionId)
        ? globalThis.String(object.clientSessionId)
        : isSet(object.client_session_id)
        ? globalThis.String(object.client_session_id)
        : "",
      editSessionId: isSet(object.editSessionId)
        ? globalThis.String(object.editSessionId)
        : isSet(object.edit_session_id)
        ? globalThis.String(object.edit_session_id)
        : "",
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
      observedItemRevision: isSet(object.observedItemRevision)
        ? globalThis.Number(object.observedItemRevision)
        : isSet(object.observed_item_revision)
        ? globalThis.Number(object.observed_item_revision)
        : 0,
    };
  },

  toJSON(message: BeginItemEdit): unknown {
    const obj: any = {};
    if (message.clientSessionId !== "") {
      obj.clientSessionId = message.clientSessionId;
    }
    if (message.editSessionId !== "") {
      obj.editSessionId = message.editSessionId;
    }
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    if (message.observedItemRevision !== 0) {
      obj.observedItemRevision = Math.round(message.observedItemRevision);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BeginItemEdit>, I>>(base?: I): BeginItemEdit {
    return BeginItemEdit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BeginItemEdit>, I>>(object: I): BeginItemEdit {
    const message = createBaseBeginItemEdit();
    message.clientSessionId = object.clientSessionId ?? "";
    message.editSessionId = object.editSessionId ?? "";
    message.entityId = object.entityId ?? "";
    message.observedItemRevision = object.observedItemRevision ?? 0;
    return message;
  },
};

function createBaseRenewItemEdit(): RenewItemEdit {
  return { clientSessionId: "", editSessionId: "", entityId: "" };
}

export const RenewItemEdit: MessageFns<RenewItemEdit> = {
  encode(message: RenewItemEdit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientSessionId !== "") {
      writer.uint32(10).string(message.clientSessionId);
    }
    if (message.editSessionId !== "") {
      writer.uint32(18).string(message.editSessionId);
    }
    if (message.entityId !== "") {
      writer.uint32(26).string(message.entityId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RenewItemEdit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRenewItemEdit();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.clientSessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.editSessionId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RenewItemEdit {
    return {
      clientSessionId: isSet(object.clientSessionId)
        ? globalThis.String(object.clientSessionId)
        : isSet(object.client_session_id)
        ? globalThis.String(object.client_session_id)
        : "",
      editSessionId: isSet(object.editSessionId)
        ? globalThis.String(object.editSessionId)
        : isSet(object.edit_session_id)
        ? globalThis.String(object.edit_session_id)
        : "",
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
    };
  },

  toJSON(message: RenewItemEdit): unknown {
    const obj: any = {};
    if (message.clientSessionId !== "") {
      obj.clientSessionId = message.clientSessionId;
    }
    if (message.editSessionId !== "") {
      obj.editSessionId = message.editSessionId;
    }
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RenewItemEdit>, I>>(base?: I): RenewItemEdit {
    return RenewItemEdit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RenewItemEdit>, I>>(object: I): RenewItemEdit {
    const message = createBaseRenewItemEdit();
    message.clientSessionId = object.clientSessionId ?? "";
    message.editSessionId = object.editSessionId ?? "";
    message.entityId = object.entityId ?? "";
    return message;
  },
};

function createBaseEndItemEdit(): EndItemEdit {
  return { clientSessionId: "", editSessionId: "", entityId: "", cancel: false };
}

export const EndItemEdit: MessageFns<EndItemEdit> = {
  encode(message: EndItemEdit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientSessionId !== "") {
      writer.uint32(10).string(message.clientSessionId);
    }
    if (message.editSessionId !== "") {
      writer.uint32(18).string(message.editSessionId);
    }
    if (message.entityId !== "") {
      writer.uint32(26).string(message.entityId);
    }
    if (message.cancel !== false) {
      writer.uint32(32).bool(message.cancel);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EndItemEdit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEndItemEdit();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.clientSessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.editSessionId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.cancel = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EndItemEdit {
    return {
      clientSessionId: isSet(object.clientSessionId)
        ? globalThis.String(object.clientSessionId)
        : isSet(object.client_session_id)
        ? globalThis.String(object.client_session_id)
        : "",
      editSessionId: isSet(object.editSessionId)
        ? globalThis.String(object.editSessionId)
        : isSet(object.edit_session_id)
        ? globalThis.String(object.edit_session_id)
        : "",
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
      cancel: isSet(object.cancel) ? globalThis.Boolean(object.cancel) : false,
    };
  },

  toJSON(message: EndItemEdit): unknown {
    const obj: any = {};
    if (message.clientSessionId !== "") {
      obj.clientSessionId = message.clientSessionId;
    }
    if (message.editSessionId !== "") {
      obj.editSessionId = message.editSessionId;
    }
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    if (message.cancel !== false) {
      obj.cancel = message.cancel;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EndItemEdit>, I>>(base?: I): EndItemEdit {
    return EndItemEdit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EndItemEdit>, I>>(object: I): EndItemEdit {
    const message = createBaseEndItemEdit();
    message.clientSessionId = object.clientSessionId ?? "";
    message.editSessionId = object.editSessionId ?? "";
    message.entityId = object.entityId ?? "";
    message.cancel = object.cancel ?? false;
    return message;
  },
};

function createBaseItemEditSessionResult(): ItemEditSessionResult {
  return {
    clientSessionId: "",
    editSessionId: "",
    entityId: "",
    status: 0,
    rejectCode: 0,
    message: "",
    itemRevision: 0,
    leaseExpiresAtUnixMs: 0,
    itemInstanceJson: new Uint8Array(0),
  };
}

export const ItemEditSessionResult: MessageFns<ItemEditSessionResult> = {
  encode(message: ItemEditSessionResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientSessionId !== "") {
      writer.uint32(10).string(message.clientSessionId);
    }
    if (message.editSessionId !== "") {
      writer.uint32(18).string(message.editSessionId);
    }
    if (message.entityId !== "") {
      writer.uint32(26).string(message.entityId);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    if (message.rejectCode !== 0) {
      writer.uint32(40).int32(message.rejectCode);
    }
    if (message.message !== "") {
      writer.uint32(50).string(message.message);
    }
    if (message.itemRevision !== 0) {
      writer.uint32(56).uint64(message.itemRevision);
    }
    if (message.leaseExpiresAtUnixMs !== 0) {
      writer.uint32(64).uint64(message.leaseExpiresAtUnixMs);
    }
    if (message.itemInstanceJson.length !== 0) {
      writer.uint32(74).bytes(message.itemInstanceJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ItemEditSessionResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseItemEditSessionResult();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.clientSessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.editSessionId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.status = reader.int32() as any;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.rejectCode = reader.int32() as any;
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.message = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.itemRevision = longToNumber(reader.uint64());
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.leaseExpiresAtUnixMs = longToNumber(reader.uint64());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.itemInstanceJson = reader.bytes();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ItemEditSessionResult {
    return {
      clientSessionId: isSet(object.clientSessionId)
        ? globalThis.String(object.clientSessionId)
        : isSet(object.client_session_id)
        ? globalThis.String(object.client_session_id)
        : "",
      editSessionId: isSet(object.editSessionId)
        ? globalThis.String(object.editSessionId)
        : isSet(object.edit_session_id)
        ? globalThis.String(object.edit_session_id)
        : "",
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
      status: isSet(object.status) ? itemEditSessionStatusFromJSON(object.status) : 0,
      rejectCode: isSet(object.rejectCode)
        ? itemMutationRejectCodeFromJSON(object.rejectCode)
        : isSet(object.reject_code)
        ? itemMutationRejectCodeFromJSON(object.reject_code)
        : 0,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      itemRevision: isSet(object.itemRevision)
        ? globalThis.Number(object.itemRevision)
        : isSet(object.item_revision)
        ? globalThis.Number(object.item_revision)
        : 0,
      leaseExpiresAtUnixMs: isSet(object.leaseExpiresAtUnixMs)
        ? globalThis.Number(object.leaseExpiresAtUnixMs)
        : isSet(object.lease_expires_at_unix_ms)
        ? globalThis.Number(object.lease_expires_at_unix_ms)
        : 0,
      itemInstanceJson: isSet(object.itemInstanceJson)
        ? bytesFromBase64(object.itemInstanceJson)
        : isSet(object.item_instance_json)
        ? bytesFromBase64(object.item_instance_json)
        : new Uint8Array(0),
    };
  },

  toJSON(message: ItemEditSessionResult): unknown {
    const obj: any = {};
    if (message.clientSessionId !== "") {
      obj.clientSessionId = message.clientSessionId;
    }
    if (message.editSessionId !== "") {
      obj.editSessionId = message.editSessionId;
    }
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    if (message.status !== 0) {
      obj.status = itemEditSessionStatusToJSON(message.status);
    }
    if (message.rejectCode !== 0) {
      obj.rejectCode = itemMutationRejectCodeToJSON(message.rejectCode);
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.itemRevision !== 0) {
      obj.itemRevision = Math.round(message.itemRevision);
    }
    if (message.leaseExpiresAtUnixMs !== 0) {
      obj.leaseExpiresAtUnixMs = Math.round(message.leaseExpiresAtUnixMs);
    }
    if (message.itemInstanceJson.length !== 0) {
      obj.itemInstanceJson = base64FromBytes(message.itemInstanceJson);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ItemEditSessionResult>, I>>(base?: I): ItemEditSessionResult {
    return ItemEditSessionResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ItemEditSessionResult>, I>>(object: I): ItemEditSessionResult {
    const message = createBaseItemEditSessionResult();
    message.clientSessionId = object.clientSessionId ?? "";
    message.editSessionId = object.editSessionId ?? "";
    message.entityId = object.entityId ?? "";
    message.status = object.status ?? 0;
    message.rejectCode = object.rejectCode ?? 0;
    message.message = object.message ?? "";
    message.itemRevision = object.itemRevision ?? 0;
    message.leaseExpiresAtUnixMs = object.leaseExpiresAtUnixMs ?? 0;
    message.itemInstanceJson = object.itemInstanceJson ?? new Uint8Array(0);
    return message;
  },
};

function createBaseItemEditPreview(): ItemEditPreview {
  return {
    clientSessionId: "",
    editSessionId: "",
    entityId: "",
    previewSequence: 0,
    position: undefined,
    rotation: 0,
    z: 0,
    scale: 0,
    revert: false,
  };
}

export const ItemEditPreview: MessageFns<ItemEditPreview> = {
  encode(message: ItemEditPreview, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.clientSessionId !== "") {
      writer.uint32(10).string(message.clientSessionId);
    }
    if (message.editSessionId !== "") {
      writer.uint32(18).string(message.editSessionId);
    }
    if (message.entityId !== "") {
      writer.uint32(26).string(message.entityId);
    }
    if (message.previewSequence !== 0) {
      writer.uint32(32).uint64(message.previewSequence);
    }
    if (message.position !== undefined) {
      Vec2.encode(message.position, writer.uint32(42).fork()).join();
    }
    if (message.rotation !== 0) {
      writer.uint32(53).float(message.rotation);
    }
    if (message.z !== 0) {
      writer.uint32(61).float(message.z);
    }
    if (message.scale !== 0) {
      writer.uint32(69).float(message.scale);
    }
    if (message.revert !== false) {
      writer.uint32(72).bool(message.revert);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ItemEditPreview {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseItemEditPreview();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.clientSessionId = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.editSessionId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.entityId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.previewSequence = longToNumber(reader.uint64());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.position = Vec2.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 53) {
              break;
            }

            message.rotation = reader.float();
            continue;
          }
          case 7: {
            if (tag !== 61) {
              break;
            }

            message.z = reader.float();
            continue;
          }
          case 8: {
            if (tag !== 69) {
              break;
            }

            message.scale = reader.float();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.revert = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ItemEditPreview {
    return {
      clientSessionId: isSet(object.clientSessionId)
        ? globalThis.String(object.clientSessionId)
        : isSet(object.client_session_id)
        ? globalThis.String(object.client_session_id)
        : "",
      editSessionId: isSet(object.editSessionId)
        ? globalThis.String(object.editSessionId)
        : isSet(object.edit_session_id)
        ? globalThis.String(object.edit_session_id)
        : "",
      entityId: isSet(object.entityId)
        ? globalThis.String(object.entityId)
        : isSet(object.entity_id)
        ? globalThis.String(object.entity_id)
        : "",
      previewSequence: isSet(object.previewSequence)
        ? globalThis.Number(object.previewSequence)
        : isSet(object.preview_sequence)
        ? globalThis.Number(object.preview_sequence)
        : 0,
      position: isSet(object.position) ? Vec2.fromJSON(object.position) : undefined,
      rotation: isSet(object.rotation) ? globalThis.Number(object.rotation) : 0,
      z: isSet(object.z) ? globalThis.Number(object.z) : 0,
      scale: isSet(object.scale) ? globalThis.Number(object.scale) : 0,
      revert: isSet(object.revert) ? globalThis.Boolean(object.revert) : false,
    };
  },

  toJSON(message: ItemEditPreview): unknown {
    const obj: any = {};
    if (message.clientSessionId !== "") {
      obj.clientSessionId = message.clientSessionId;
    }
    if (message.editSessionId !== "") {
      obj.editSessionId = message.editSessionId;
    }
    if (message.entityId !== "") {
      obj.entityId = message.entityId;
    }
    if (message.previewSequence !== 0) {
      obj.previewSequence = Math.round(message.previewSequence);
    }
    if (message.position !== undefined) {
      obj.position = Vec2.toJSON(message.position);
    }
    if (message.rotation !== 0) {
      obj.rotation = message.rotation;
    }
    if (message.z !== 0) {
      obj.z = message.z;
    }
    if (message.scale !== 0) {
      obj.scale = message.scale;
    }
    if (message.revert !== false) {
      obj.revert = message.revert;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ItemEditPreview>, I>>(base?: I): ItemEditPreview {
    return ItemEditPreview.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ItemEditPreview>, I>>(object: I): ItemEditPreview {
    const message = createBaseItemEditPreview();
    message.clientSessionId = object.clientSessionId ?? "";
    message.editSessionId = object.editSessionId ?? "";
    message.entityId = object.entityId ?? "";
    message.previewSequence = object.previewSequence ?? 0;
    message.position = (object.position !== undefined && object.position !== null)
      ? Vec2.fromPartial(object.position)
      : undefined;
    message.rotation = object.rotation ?? 0;
    message.z = object.z ?? 0;
    message.scale = object.scale ?? 0;
    message.revert = object.revert ?? false;
    return message;
  },
};

function createBaseCheckpoint(): Checkpoint {
  return { checkpointRevision: 0, tick: 0, snapshotJson: new Uint8Array(0), final: false };
}

export const Checkpoint: MessageFns<Checkpoint> = {
  encode(message: Checkpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.checkpointRevision !== 0) {
      writer.uint32(8).uint64(message.checkpointRevision);
    }
    if (message.tick !== 0) {
      writer.uint32(16).uint64(message.tick);
    }
    if (message.snapshotJson.length !== 0) {
      writer.uint32(26).bytes(message.snapshotJson);
    }
    if (message.final !== false) {
      writer.uint32(32).bool(message.final);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Checkpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCheckpoint();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.checkpointRevision = longToNumber(reader.uint64());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.tick = longToNumber(reader.uint64());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.snapshotJson = reader.bytes();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.final = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Checkpoint {
    return {
      checkpointRevision: isSet(object.checkpointRevision)
        ? globalThis.Number(object.checkpointRevision)
        : isSet(object.checkpoint_revision)
        ? globalThis.Number(object.checkpoint_revision)
        : 0,
      tick: isSet(object.tick) ? globalThis.Number(object.tick) : 0,
      snapshotJson: isSet(object.snapshotJson)
        ? bytesFromBase64(object.snapshotJson)
        : isSet(object.snapshot_json)
        ? bytesFromBase64(object.snapshot_json)
        : new Uint8Array(0),
      final: isSet(object.final) ? globalThis.Boolean(object.final) : false,
    };
  },

  toJSON(message: Checkpoint): unknown {
    const obj: any = {};
    if (message.checkpointRevision !== 0) {
      obj.checkpointRevision = Math.round(message.checkpointRevision);
    }
    if (message.tick !== 0) {
      obj.tick = Math.round(message.tick);
    }
    if (message.snapshotJson.length !== 0) {
      obj.snapshotJson = base64FromBytes(message.snapshotJson);
    }
    if (message.final !== false) {
      obj.final = message.final;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Checkpoint>, I>>(base?: I): Checkpoint {
    return Checkpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Checkpoint>, I>>(object: I): Checkpoint {
    const message = createBaseCheckpoint();
    message.checkpointRevision = object.checkpointRevision ?? 0;
    message.tick = object.tick ?? 0;
    message.snapshotJson = object.snapshotJson ?? new Uint8Array(0);
    message.final = object.final ?? false;
    return message;
  },
};

function createBaseProtocolError(): ProtocolError {
  return { code: "", message: "", serverProtocolVersion: 0 };
}

export const ProtocolError: MessageFns<ProtocolError> = {
  encode(message: ProtocolError, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    if (message.serverProtocolVersion !== 0) {
      writer.uint32(24).uint32(message.serverProtocolVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProtocolError {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseProtocolError();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.code = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.message = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.serverProtocolVersion = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ProtocolError {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      serverProtocolVersion: isSet(object.serverProtocolVersion)
        ? globalThis.Number(object.serverProtocolVersion)
        : isSet(object.server_protocol_version)
        ? globalThis.Number(object.server_protocol_version)
        : 0,
    };
  },

  toJSON(message: ProtocolError): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.serverProtocolVersion !== 0) {
      obj.serverProtocolVersion = Math.round(message.serverProtocolVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProtocolError>, I>>(base?: I): ProtocolError {
    return ProtocolError.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProtocolError>, I>>(object: I): ProtocolError {
    const message = createBaseProtocolError();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    message.serverProtocolVersion = object.serverProtocolVersion ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
