{
  "name": "canvas-physics",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "test": "vitest run",
    "dev": "pnpm --filter @canvas-physics/demo dev"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "ts-proto": "^2.12.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
